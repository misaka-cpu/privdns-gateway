# 更新日志

本项目按语义化 `v1.x` tag 正式发布;以下按版本/日期记录主要变化,完整提交见 git 历史。

## 2026-08-31 — v1.11.7(mihomo 的完整性判据一直在说谎)

**建议升。** 没有配置格式变化, 升完不需要做任何事。这一版**不动任何运行行为** —— 改的是
"怎么判断跑着的内核是不是官方那一份", 以及判错时 `pdg update` 还修不修得回来。

### 四重假绿, 每一条都在这一轮实测复现过

判断 mihomo 是否已是钉死版, 以前走的是 `pdg_mihomo_is_version`。它有三个问题, 叠在一起:

1. **它问的是 PATH 上的 `mihomo`, 而 systemd 执行的是 `/usr/local/bin/mihomo`。**
   PATH 上放一个自报 v1.19.30 的壳, 判据就答"已是钉死版" —— 而真正在跑的那个文件可以是
   缺失、旧版, 或者内容被换过。实测: 绝对路径三种状态全部被"接受"。
2. **它只比自报版本**, 而版本是二进制自己打印的字符串。换掉内容、留住版本串, 安装与换核
   都会跳过整段下载, 日志上连"下载 mihomo"这行都不会出现。
3. **doctor 对任何能解析出来的版本都报绿**, 文案还写"版本随项目发布更新" —— 实测 v0.0.1
   也是绿的; 命令退出码非零但输出里带个版本号, 同样绿。
4. 更要命的第四条: **仓库最新 + 已装项目文件一致 + 内核内容漂移**这个现场, `pdg update`
   会在顶层「已是最新」短路直接返回 0, 副作用日志一行都没有 —— 而 `pdg update` 正是这台
   机器上唯一能修好它的东西。修复路径被它自己堵死了。

### 现在的判据

`lib/versions.sh` 新增 `[mihomo-bin-amd64]` / `[mihomo-bin-arm64]`, 钉的是**解压后二进制**
本身。原先只钉下载归档 —— 而归档钉值只在"真的下载了"那条路上起作用, 被上面的短路一跳过就
永远用不上; 落盘的那个文件, 项目从来没钉过。

新判据 `pdg_mihomo_binary_ok <架构> [版本] [路径]` 只有四件事同时成立才放行:
**该架构有钉值 / 文件存在且可执行 / `-v` 退出码为 0 且自报版本精确相等 / 内容 SHA256 等于
钉值**。退出码单独取、不经管道 —— `$(cmd | head -1)` 的退出码是 `head` 的, 内核自己非零会
被吞掉, 而那正是"命令失败但打印了版本号"这一形态要挡的。

三处安装点全部换用它: `install.sh`、`_update_core_binary`, 以及 `_activate_mihomo_core`
(sing-box 老机迁移那条路 —— 它是新测试替我找出来的第三处)。三处都在**落盘之前**核解压产物。

### doctor 分成两条, 措辞能区分

- **`mihomo 版本`** —— 只说自报版本。绝对路径缺失 / 退出码非零 / 读不出版本 / 与钉死版
  不符 → fail; 读不到钉死版本 → warn 且明说"本项无结论"。
- **`mihomo 二进制`** —— 只说文件内容。摘要不符 → fail 且点明是**内容**不符; 架构不在钉值
  表里或读不到钉值 → warn + 无结论。

"版本对但内容不对"因此会被单独看见, 而那正是最该被看见的一种。

### 顺带修好的两处(同一个缺陷的两半)

- **顶层「已是最新」门现在把内核二进制也算作已装文件。** 内容漂移不再让 `pdg update` 变成
  空操作。**mosdns 一并纳入** —— 同一行代码里的同一个缺陷, 只修一半没有道理。
  (mosdns 的解析行为、版本、配置与换版路径**没有任何改动**。)
- **快照现在收 `/usr/local/bin/mihomo`。** 换核之后若有别的步骤失败, 回滚要退得回去。

### `tools/bump-kernel.sh` 改成原子写

它要改 3~6 行, 其中大多数是 64 位十六进制。以前逐次直写正式文件 —— 中途任何一次失败都会
把它停在"版本换了、第二个哈希没换"的半成品上, 而那种状态会在装机时 die 在 SHA 校验上,
报错指向供应链异常, 现场根本看不出是工具写了一半。

现在: 在**目标同目录**的暂存副本上改(跨文件系统时 `mv` 不是原子的), 锚点唯一性、`bash -n`、
全字段回读、与前像对账全部通过之后, 才一次 `mv` 替换。任意普通失败下正式文件逐字节不变。

另外: 下载物与 **GitHub 的 asset digest** 交叉核对, 按精确 tag + 精确资产名取 —— mihomo
同一个 tag 下 amd64 家族有二十多个相似名(`-compatible-` / `-v1-` / `-v2-` / `-v3-` /
`-go120-` / `-go123-`, 外加 `.deb`/`.rpm`/`.pkg.tar.zst`), 通配一下就会核错文件。
**这条 digest 是额外的交叉证据, 不是独立的签名信任链** —— 文件与摘要由同一方托管, 上游
两家都不发布独立签名校验文件, 底子仍是首次下载即信任。

非本机架构改读**完整 ELF 头**(magic / class / endianness / e_machine), 只读 `e_machine`
两个字节的话, 一个伪造那两字节的非 ELF 文件就能过关。`PDG_BUMP_SKIP_VERIFY` 只能与测试
取件器同时使用, 官方下载路径拒绝跳过校验。

### 测试与夹具

新增: mihomo 完整性闭包、doctor 四态、**真实换核**(前像 v1.19.29 → 钉死版, 用真内核跑真
`mihomo -t`; 成功 / 配置检查失败 / 起来不稳三态都验旧核逐字节还原、无 `.prev`、无错误的
成功文案)、E2E mihomo 夹具闭包, 以及两支负控(仅本地手工跑, 不进 CI —— 它们要反复改写
生产文件)。

夹具侧同时补了两个洞, 都是这一轮被 CI 逼出来的:

- 新夹具建沙箱仓库时裸调 `git config/add/commit`, 绕过了 `e2e_git`。那道守卫源于 07-31
  一次 e2e 改写共享仓库 tag/remote 的事故, 现在两处都收口了。
- 五支 E2E 用例各自内联一个"只 echo 版本号"的 shell 桩 mihomo。判据一旦看内容, 桩就被
  正确拒绝 → 安装真的去下载 → 撞上用例自己装的假 curl(对 `*.gz` 写字面量 `stub`)。
  现在统一走 `e2e_seed_mihomo_bin`, 播**真钉死版**, 语义与 `e2e_seed_mosdns_bin` 一致;
  并加了静态守卫, 桩再长回来就转红。取件复用既有机制: mihomo 并入同一个 artifact,
  全 run 官方 Release 取件仍是**一次**, 每个 matrix 格 0 次。

### 仍然没解决的(不阻塞本次发布)

- provider 的运行期证据: `mihomo -t` **不加载 provider**(文件根本不存在也报 test
  successful), 服务 active 也不证明 provider 有效; 唯一的运行期信号在 `127.0.0.1:9090`
  的 clash API, 而它在面板关闭时无认证。`check_rulesets` 的"格式均可被 mihomo 加载"
  因此仍不够真实 —— 这一版**一个字节都没改它**, 留待架构裁决。
- `install.sh` 的 mihomo `curl` 缺独立错误分类: 下载失败/截断与"摘要不符"报成同一种错。
- 仓库里约 63 处 `pipefail` + `grep -q` 同形态(`grep -q` 命中即关管道 → 上游 SIGPIPE →
  整条管道判失败 → **条件反转**)。本轮只修了自己踩到的那两处。

## 2026-08-30 — v1.11.6(流量内核升到 mihomo v1.19.30)

**建议升。** 没有配置格式变化, 升完不需要做任何事。`pdg update` 会顺带把内核换掉 ——
换核失败会自动还原旧核, 整次更新还有快照与自检门兜着。

### mihomo v1.19.29 → v1.19.30

上游这一版 83 笔提交, 其中一批正落在这台网关的命脉上。本项目整条分流链依赖
`sniffer.override-destination`(手机把流量交给网关时目标地址已经丢了, 靠嗅探还原), 而
v1.19.30 集中改了嗅探:

- 支持 H2C 与 QUICv2 嗅探
- 嗅探器支持多轮读取, 能跨多个 TLS 记录拼装 ClientHello(以前拆包拆得碎就嗅不出来)
- 初次嗅探失败后不再直接断连
- 重叠端口上的多个 TCP 嗅探器互相协调
- HTTP/2 嗅探不再做无谓等待

外加 `crypto/tls` 的 CVE-2026-56862 修复。

**一处要留意的**: 上游这版开始**严格校验 Clash 风格的域名通配符**。本项目自己生成的配置
只用 `DOMAIN-SUFFIX` / `DOMAIN` / `DOMAIN-KEYWORD` / `RULE-SET`, 不产 `+.` / `*.` 形态,
所以不受影响。但**第三方规则集是下载来的用户内容** —— 如果某份表里有不合语法的通配符,
新版会拒绝加载**那一份 provider**(不是整份配置)。升完如果发现某个规则集不生效, 先看
`journalctl -u mihomo` 里有没有该 provider 的解析报错。

### 换内核版本的工具

新增 `tools/bump-kernel.sh` —— 维护用, 不装到网关上, 对使用者没有任何影响。

换版本身只是改三行, 但其中两行是 64 位十六进制、两个架构各一份(mosdns 还多一份解压后
二进制的)。手工粘错一位的后果不是"少了点什么", 而是装机直接停在 SHA 校验上, 而报错指向
供应链异常 —— 现场看不出那是笔误。现在这一步交给机器:

    tools/bump-kernel.sh mihomo v1.19.30

它取件、核对"这份东西确实是要的那个版本"(本机架构真跑一次比对自报版本; 跑不动的那个架构
读 ELF 头的机器类型, 防两次下成同一个架构)、算钉值、改写 `lib/versions.sh` 并打印 diff。
**只改钉值** —— 不提交、不推送、不发版。信任模型没变: 两家上游都不发布校验和文件, 仍是
从官方 Release 页首次下载即信任, 工具只是把"下载→核对→誊写"这条链自动化。

本版的换核就是它的第一次真实使用。

### 已知边界

- mosdns 仍是 v5.3.4, 本版没动它。
- 上一版留下的边界依旧: 更新前的预检读**当前**仓库钉值, 更新后 doctor 按**新**钉值判断 ——
  这次能顺利换核是因为 `_update_core_binary` 会主动把内核装到钉死版本, 与 mosdns 那条路
  不是同一回事; mosdns 的换版路径仍未打通。

## 2026-08-29 — v1.11.5(`pdg update` 不再把机器悄悄退回旧版; mosdns 出问题时不再白折腾一遍)

**建议升。** 没有配置格式变化, 升完不需要做任何事; 去广告仍然默认关闭。

### `pdg update` 不再把领先发布的机器静默退回去

以前的判据只有一条: HEAD **恰好等于**最新发布 tag 就跳过, 否则一律 `git reset --hard <tag>`。
而 `reset --hard` 对方向一视同仁 —— 机器上跑着比最新 Release 更新的提交时(比如临时装了尚未
发布的修复), 一次普通的 `pdg update` 会把它**退回上一个版本**, 屏幕上还打印「✅ 已更新」。
不提示、不确认, 事后只能从文件时间和 git reflog 里倒推发生过什么。

现在先判方向, 再决定动作:

| 当前版本与最新发布的关系 | 行为 |
|---|---|
| 相同 | 文件都一致就直接跳过, 不留快照、不重启服务 |
| 落后 | 正常升级 |
| **领先(跑着未发布的提交)** | **拒绝, 并说清原因** |
| **分叉** | **拒绝, 不猜方向** |
| 判不出关系(仓库损坏等) | 拒绝 |

拒绝时一个文件都不动: 不建快照、不 reset、不装文件、不迁移、不重启。真要退回某个版本,
走 `pdg rollback` 或重装那一版 —— update 不再兼作降级工具。`PDG_UPDATE_FORCE=1`(强制重装
同一版本)照旧可用, 但它**不是**降级开关, 不会绕过这道门。

`pdg update --dry-run` 也跟着改了: 以前无论什么关系都打印一段「待更新提交(HEAD..tag)」,
而领先时那个区间是空的 —— 屏幕上只剩一个标题, 读起来正好是「已经最新了」。现在按关系分别
说明, 领先时会列出领先的提交并写明正式 update 会拒绝。

### 更新前先看一眼 mosdns, 不合法就停在动手之前

这一版给 doctor 新增了「mosdns 二进制内容与官方钉值是否一致」的判据。但 `pdg update` 的自检
是在**更新做完之后**才跑的 —— 于是一台 mosdns 已经不对的机器会先建快照、切代码、装文件、
跑迁移、重启服务, 走完全程, 最后被自检判红再整个回滚。动一遍又回到起点, 而且每跑一次
`pdg update` 就重复一遍。

现在更新前先问一次。不合法时具名说清是哪一类 —— 文件不存在 / 不可执行 / `mosdns version`
返回非零 / 读不出版本 / 自报版本不符 / 内容摘要不符 —— 然后停下, 快照、reset、装文件、迁移、
重启一样都不做。修好再更新即可, `sudo pdg doctor` 会指出同一项。

更新过程中把 mosdns 弄坏的情形没有放松: 更新后的自检照样判红并回滚。

### mosdns 的健康判据不再接受几种假绿

三处都会让 doctor 在实际有问题时报绿:

- **退出码被无视**: 判据把命令的正常输出和错误输出拼起来找版本号, 找到就算数。于是一个
  起不来的 mosdns(退出码非 0、错误里写着 fatal)只要还能打印自己的版本, 就会被判成 ✓。
  现在退出码非 0 直接判「无结论」, 且只认正常输出里的版本号。
- **问错了文件**: 判据调的是 PATH 上的 `mosdns`, 而 systemd 实际执行的是
  `/usr/local/bin/mosdns`。PATH 前面有别的同名程序时, 报的就不是网关在跑的那个。现在固定
  问 systemd 执行的那个文件。
- **内容从来没被校验过**: 项目原先只钉了下载压缩包的哈希, 而安装器「自报版本相同就跳过
  下载」—— 一个内容不同、却自报同样版本的二进制会让整段安装被跳过, 连带跳过供应链校验,
  落盘的那个文件本身从来没被钉过。现在 `lib/versions.sh` 增加了**解压后二进制**的 SHA256
  (amd64 与 arm64 各一份), 安装器的跳过条件改成「版本相同**且**摘要相符」, doctor 也新增
  一条对应判据: 内容不符判红, 架构不在钉值表里或读不到钉值判「无结论」。

> mosdns 本身**没有升版**, 仍是 v5.3.4。这一版改的是「怎么判断它是不是官方那一份」。

### 去广告状态读不出来时, 不再说成「未启用」

启用位存在 `/etc/privdns-gateway/profile.env` 里, 权限是 `0600 root:root`。以前的读取把
四件不同的事折成同一个空值 —— 文件不存在、文件读不出来、值看不懂、真的没启用 —— 再统一
显示成「未启用」。结果同一台**开着**去广告的机器, 用 root 看是「已启用」, 换个身份看是
「未启用」。一个权限问题, 长得和「这台机器没开去广告」一模一样。

现在分三种说法: 已启用 / 未启用 / **状态未知**。读不出来时明说是读不到, 并提示跑
`sudo pdg doctor`。

同一处还修了另一个形态: 启用位是开着的、但规则文件缺失或读不出来时, 以前显示「第三方表
0 条」。那不是 0 条 —— 0 条是「表在, 里面没东西」, 缺文件是「表根本不在」(mosdns 会起不来)。
两者显示成同一句话, 现场没法区分。现在会明说是规则文件的问题。

### 测试与 CI(不影响运行中的网关)

- E2E 夹具以前用一个「只会打印版本号」的假 mosdns, 或者干脆不放这个文件。假的二进制正是
  上面那两条新判据要抓的形态 —— 夹具替被测代码说谎, 把问题藏了很久。现在健康夹具一律使用
  真实钉定的二进制, 并在放好之后用生产判据复核一次。顺带发现一个从没被登记过的用例:
  它串行跑多支 E2E, 取件动作藏在夹具内部、日志上完全看不见, 现在也走同一条路。
- CI 里 mosdns 的官方取件原先散在六个 job 里, 而其中一个是 21 格的矩阵 —— 每格一个独立
  容器, 于是「放在 job 层」实际就是「每个用例一次」, 一次 CI 会向上游发 28 次请求。现在
  收敛成每次 CI、每种架构**只取一次**, 校验后经这次 CI 内部的临时产物分发给各个用例;
  每个用例拿到后自己重算 SHA256、交叉核对随行清单、复核自报版本, 再过一遍生产判据, 任一
  不符即失败。

### 已知边界

- **未来 mosdns 换版本的路径仍未打通。** 更新前的预检读的是**当前**仓库里的钉值, 而更新
  之后 doctor 按**新**钉值判断 —— 真到了要升 mosdns 那天, 这条路还需要单独补。本版没有
  解决它, 也没有升 mosdns。
- 本版不涉及内网面板、Tailscale、流量内核、去广告规则引擎与证书链路。

## 2026-08-28 — v1.11.4(Telegram 里终于能管规则集了)

**用 Telegram 管去广告的建议升。**没有配置格式变化, 升完不需要做任何事, 去广告仍然默认关闭。

### 规则集、启用、更新, 现在都在 Bot 里

v1.11.2 做了「第三方源可配」, 但那只有命令行能用 —— Telegram 的去广告菜单里只有 状态 /
加规则 / 删规则 / 查域名 四项。也就是说用户看得到「去广告」这个菜单, 却**没有任何一条路能把
一份新表用起来**: 规则集加不了, 去广告也启用不了、更新不了。

半个功能比没有更坏 —— 用户以为自己在管理去广告, 实际只能往一份**永远不会被下载**的表里加
单条域名。这一版补齐:

```
📊 当前状态
🟢 启用            🔴 停用                    ← 新
➕ 添加阻断规则     ➖ 删除阻断规则
🔎 查询域名
📚 规则集(第三方源)                          ← 新
↩️ 返回
```

规则集子菜单: 列出当前生效的源 + 添加 / 删除 / 恢复内置默认 / **立即更新(下载并生效)**。

**为什么把启用与更新一起做进来**: 只加「添加规则集」仍然是半个功能 —— 加完没有任何一条路
能让它生效。这三件事缺任何一件, 「换一份表」这条路都走不完。

### 几处刻意的设计

**删除不用粘长 URL。** 列出当前的源, 每条一个按钮。按钮里放的是**下标** —— Telegram 的
callback 数据只有 64 字节, URL 放不下。下标在**点的那一刻**重新对照当前列表: 渲染与点击
之间列表可能被另一条会话或命令行改过, 拿旧下标直接删等于删掉另一条。对不上就拒绝, 让你
重新进一次。

**慢操作在后台跑。** 更新要真下载、可能重启 DNS, 同步执行会把 Bot 卡住。

**子菜单正文直接列出当前源**, 并明说两件最容易误解的事: 改完要点「立即更新」才生效;
自定义源是**替换**内置默认, 不是追加。

### 已知边界

- 用 DoT 的客户端与用明文 53 的客户端, 去广告行为一致(阻断在 mosdns 里做), 这一版没有变化。
- 去广告仍然**没有命中统计**(拦了多少次、哪些域名拦得最多)。四条实现路径都调查过并否决 ——
  拿到统计的唯一途径都要求这台机器持有一份「谁解析了什么」, 而那正是本项目承诺不记录的东西。
- 源管理与批量加规则只有命令行与 Telegram, 没有网页入口。
- 更新完整的第三方表仍然会重启一次 mosdns, 期间有短暂解析中断。
- 低内存模式只缩小 mosdns 的 DNS 缓存与 journald 上限, **不会缩小去广告的域名集**。
- provider 仍只支持 5 项; 表外的一律 fail-closed 拒绝启用。
- Tailscale 卸载后 `net.ipv4.conf.all.src_valid_mark` 仍是 1 —— 那是 Tailscale 自己的行为,
  本项目**有意不替它还原**(它还在跑时改回 0 会弄坏它的网络); doctor 会在确实卸干净之后报出来。

## 2026-08-28 — v1.11.3(批量加规则; 一批"存在但没人知道"的收尾)

**没有配置格式变化, 升完不需要做任何事**, 去广告仍然默认关闭。这一版没有新的大功能, 是把
一批**已经在那儿、只是没人发现**的问题清掉。

### Telegram 一次可以加多个域名了

以前一次只能加一个: 想加十个就得点十次、发十条 —— 而每一次都是一整套 抢全局锁 → 改源 →
重编译 → **重启 mosdns**。十条规则等于十次 DNS 中断, 中间任何一次撞上 `pdg update` 就卡住。

现在一条消息里换行或空格分隔发多个(一次最多 100 个), 走**一笔事务**: 一次锁、一次编译、
一次重启。命令行同样可用:

```bash
printf 'a.example\nb.example\n' | sudo pdg adblock rule-add-many
```

**逐条给结果, 不整批拒**: 粘 20 个域名其中一个打错就整批退回的话, 用户还得自己去找是哪一个
—— 那正是他想让机器替他做的事。合法的照收, 不合法的逐条点名。

一次 100 个这个上限不是内存约束(那由下面那条兜着), 是回执可读性与事务时长 —— 粘一屏远超
这个数的, 那是在导入一份表, 该走 `pdg adblock source add`。

### 自定义规则有了体积上限(50000 条 / 2 MiB)

第三方表一直有两道硬顶(8 MiB / 50 万条), 因为那两个数字是按"整机 512 MiB 可用"实测定出来
的 —— mosdns 把域名集全量装进内存, 超了就是 OOM, 而 OOM 的表现是**整台机器的 DNS 没了**。

用户自己那份规则文件**一道都没有**, 而它逐字节进同一个内存预算。规则能脚本化追加, 一个循环
写岔、或者把一份下载来的表直接追加进去就到了; 触发之后没有任何提示, 只在下一次启用或 mosdns
重启时把 DNS 打没。

50000 条按同一次实测折算约 6.7 MiB, 相对余量不到 4%; 手工维护的名单到不了这个量级。到顶时:
加规则当场拒(不拖到启用时才报)、**删规则仍然可用**(拦住"加"却连"删"也拦住就是把人锁死)、
关闭态不受影响(用户源一个字节不动)。

**"规则满了"不再被说成"域名不合法"** —— 那是两件事。以前 Bot 会对一个完全合法的域名说它
不合法, 用户照着那句去改域名, 改多少次都没用, 而真正要做的是删几条旧规则。

### `pdg ssh-source tailnet` 跟得上自定义端口了

放行的 UDP 端口一直写死 41641。那只是 Tailscale 的**官方默认值** —— 它是可配的
(`/etc/default/tailscaled` 的 `PORT=`)。改过端口的机器上这条放行**双向失效**: 41641 那条
没有监听者, 而真正在用的端口被防火墙挡掉。于是这条命令当初要消除的冷启动窗口原样回来 ——
空闲几小时后出事想连进去, 第一次 SSH 必超时, 而那正是它存在的全部理由。

现在端口从 tailscaled 的配置派生(与 doctor 的对账判据共用同一份解析), 取不到就退回 41641
并**把原因说出来**。

一并修掉三处按端口号认的判据, 都不是文案问题:
- 切回 `any` 时那条删不掉 —— "已恢复对全网放行"说完, 机器上还留着一条没人知道的 UDP 放行;
- 状态显示一直报"41641 未放行, 跑一次 tailnet 修复", 而跑完还是这句;
- "已经是 tailnet"判成"还没收紧", 于是重复执行并**再起一次自动回退定时器**。

### 面板反代不再为一份没变的配置重启

内网面板的三个派生产物在产物层早就是逐字节幂等的, 但每一次 `pdg update`、每一次面板同步都
照样重启一次反代 —— 重启期间反代是断的, 手机上正开着的面板会掉一次, 而这类重启没有任何事情
能从中获益。现在产物真变才重启; 跳过重启的那条路上仍会把出站白名单补回内核(它本来是靠重启
带进去的, 少这一步会留下一个反代能连内网任意地址的窗口)。

### `pdg` 的用法里终于列出了 `lan` 和 `adblock`

打错子命令时唯一的自助线索就是那一行用法串, 而它漏了两条: `lan` 从 v1.9 起、`adblock` 从
v1.11 起 —— 命令一直能用, 只是帮助里看不见。现在加了一格守卫遍历真实的命令表, 再漏就在 CI 红。

### 已知边界

- 面板反代"产物没变就不重启"这条路径**至今没在真机上触发过**(两台线上机的升级都走正常
  路径, 那段收敛只在回滚时跑)。有测试覆盖, 线上验证仍缺。
- 源管理与批量加规则目前只有 CLI 与 Telegram, 没有网页入口。
- 更新完整的第三方表仍然会重启一次 mosdns, 期间有短暂解析中断。
- 低内存模式只缩小 mosdns 的 DNS 缓存与 journald 上限, **不会缩小去广告的域名集**。
- provider 仍只支持 5 项; 表外的一律 fail-closed 拒绝启用。
- Tailscale 卸载后 `net.ipv4.conf.all.src_valid_mark` 仍是 1。那是 Tailscale 自己的行为,
  **本项目有意不替它还原**(Tailscale 还在跑时改回 0 会弄坏它的网络); doctor 会在确实卸干净
  之后报出来并给出命令。
- 是否把阻断改到 mihomo 规则引擎 —— 尚未裁决。

## 2026-08-27 — v1.11.2(第三方源可以自己配了; 顺带修一条假警告)

**用去广告的建议升; 没用的这一版只有一条假警告的修复。**没有配置格式变化, 升完不需要做任何事,
去广告仍然默认关闭。

### 第三方源可以自己配了

```bash
sudo pdg adblock source list
sudo pdg adblock source add https://example.com/list.txt
sudo pdg adblock source del https://example.com/list.txt
sudo pdg adblock source reset
```

源存在 `/etc/privdns-gateway/adblock-sources.txt`, 一行一个, 允许 `#` 注释。**没配就沿用内置
默认**(anti-AD), 所以升上来行为一个字节不变。配了就是**替换**内置默认 —— 不是追加, 否则内置源
永远删不掉; `source list` 会同时列出当前生效的与内置默认, 不让这个语义变成暗箱。

用户源是**用户数据**, 进版本快照: 换过源的机器回滚后要是悄悄退回内置默认, 现场看着一切正常,
拉的却是另一张表。恢复时这份清单**逐行校验**(只接受 https、默认 443、主机名是合法域名) ——
它决定了更新会往哪儿发请求, 一份被换过的快照不该借着回滚把任意 URL 塞进来; 有一行不合格就
整笔恢复拒绝, 现网一个字节不动。

多源顺序回退**本来就有**(第一个可用的胜出), 这一版补的只是"让用户能把源传进去"这半边。
「anti-AD 单一默认源, 无自动回退」那条已知边界到此关闭。

**安全边界只动了一条**: 允许连接的主机名从"内置默认算出来的封闭集合"改成"**当前生效的源**算
出来的集合"。`source add` 时当场校验 URL(只接受 https、默认 443、主机名是合法域名, 不能是 IP
字面量), 不合规的进不了文件, 也就进不了这个集合 —— **没 add 过的主机照样连不上**。零重定向、
非公网地址拒绝、DNS 与连接地址绑定、TLS 用原始主机名校验、8 MiB 体积上限、条目数上下限与骤降
比, 一条都没松。

### 上游表里的零星怪行不再废掉整张表

实测一张 215320 行的第三方合并表, 现有解析器**一条都收不进来**, 两个原因:

- 有一行主机名带下划线(`fb_servpub-a.akamaihd.net`)。DNS 协议本身允许下划线
  (`_dmarc` / `_acme-challenge` 就是), 只是 RFC 1123 的 **hostname** 规范不允许; 作为阻断规则
  的模式串, 下划线不造成歧义也不造成注入。**本版本起放行下划线, 且只放行下划线** —— 通配符、
  路径、ABP、正则仍然整份拒。
- 表里混着 57 条纯 IPv4。合并型表从多个上游拼起来, 掺进几条 IP 是常态, 而 `domain_set` 里放一个
  IPv4 字面量, mosdns 会拿它当域名匹配 —— 永远匹配不到真实查询, **无害也无用**。

原来的规矩是"一行不合格就整份拒"。那条规矩的核心担忧是对的(部分解析出来的表少了多少条没人
知道), 但对第三方表有个副作用: 用户对上游没有控制权, 上游一行手滑就整张表用不了。**本版本起
改成: 逐行的域名不合格(含纯 IP / localhost)跳过并计数, 跳过比例超过 1% 仍然整份拒。** 跳过数
会写进状态元数据, 不静默丢。而**结构性**不对 —— HTML 错页、ABP / 正则语法、认不出的行形态 ——
照旧整份拒: 那不是"有几行坏", 是"这压根不是我们要的文件"。

### 规则条数上限 15 万 → 50 万

旧上限出自更早的一次测量, 那次的约束是 `MemoryMax=96M` —— 一个测试时人为加的上限, 产品并不设
它。按"整机 512 MiB 可用"重新实测(真跑 mosdns v5.3.4 + 生产模板 + 14.5 万条 geosite):

| 条数 | 15 万 | 30 万 | **50 万** | 60 万 | 80 万 | 100 万 |
|---|---|---|---|---|---|---|
| mosdns RSS | 61.6 | 77.8 | **108.6** | 109.5 | 123.0 | 166.7 MiB |

选 50 万是因为它正好在一个台阶顶上(50 万与 60 万的内存几乎一样), 再多一点不会突然多吃内存。
512 MiB 的机器上非 mosdns 部分实测约 217 MiB, 50 万条时整机约 326 MiB, **余量 186 MiB 且不依赖
swap**。

**延迟与条数无关**: 150 qps 定速下 p50 稳在 0.11 ms、p95 0.18 ms, 从 15 万到 100 万看不出趋势 ——
域名集的查询开销取决于域名长度, 不是表的大小。约束只在内存。

### 自检不再对着正常的限流器报警

`pdg doctor` 的限流判据会对一台配置**完全正确**的机器报「rate_limiter 缺失或参数/动作异常」,
还建议去跑 `pdg restart` 触发迁移 —— 追一个不存在的问题。

原因是 `!$client_limiter` 那一行有**两个生产者**, 写出来的形态不同: 模板带行尾注释, 迁移写出来
的不带。判据原本容忍行尾注释; v1.11.0 为了让"限流是否排在缓存之前"这个位置判断不被注释骗过,
改成在剥过注释的文本上匹配, 顺手把那段容忍去掉了 —— 而剥注释只管整行, 行尾注释原样留着。
于是模板形态(= 全新装机的形态)一律被误判。现在行尾注释一并剥掉: 位置判断照样不被骗, 两种形态
也都认得。功能从未受影响 —— 限流由 mosdns 执行, doctor 只是旁观者。

### 已知边界

- 源管理目前**只有 CLI**, Telegram Bot 里还没有入口。
- 更新完整的第三方表仍然会重启一次 mosdns, 期间有短暂解析中断。
- 低内存模式只缩小 mosdns 的 DNS 缓存与 journald 上限, **不会缩小去广告的域名集**。
- 用户自己的 block 文件仍然没有体积硬上限(第三方表有 8 MiB 硬顶)。
- provider 仍只支持 5 项; 表外的一律 fail-closed 拒绝启用。
- Telegram 内联按钮一次只能加一个域名。
- 是否把阻断改到 mihomo 规则引擎 —— 尚未裁决。

## 2026-08-27 — v1.11.1(v1.11.0 的存量机器根本装不上去)

**在 v1.10.x 上的必升 —— v1.11.0 你多半装不上。**没有配置格式变化, 升完不需要做任何事;
去广告仍然默认关闭。

**问题**: v1.11.0 的去广告迁移拿**一行注释**当受管块的插入锚点 ——
`  # MITM 接管域名的劫持序列`。那行注释只存在于仓库模板里(2026-07-20 才加进去), **没有任何
迁移会把它写进现网配置**。于是只有"那之后全新装机、由模板渲染出配置"的机器才有它; 存量机器
的配置是老模板加一串迁移堆出来的, 一律没有。

结果是升级走到迁移这一步就停住:

    现网配置里找不到插入锚点(这台的 mosdns 配置形态不认识)
      ❌ 去广告受管块候选生成失败, 现网未改动。
    迁移(__migrate)失败, 回滚到更新前快照…

fail-closed 与回滚本身是好的 —— **现网一个字节没被改, 机器仍然健康, 服务照常**, 只是版本停在
原地。但这台机器从此升不上去, 每次 `pdg update` 都会重演一遍。

**修法**: 锚点换成 `  - tag: force_hijack_seq` 这个**定义行** —— 它是迁移真正写出来的结构,
不随注释文案变。插入位置没有变化。仓库里其实早就写下过这条规矩(`tests/helpers/` 里那句
"不拿注释文案当锚点"), 这次是自己违反了它。

**顺带修掉一处残留**: 候选生成失败时, 规则文件目录会被提前建出来, 留下三个 0 字节文件。而
doctor 判断"这台机器有没有用过去广告"正是看这个目录存不存在 —— 一次失败的迁移把这个问题
搅浑了。现在规则文件挪到候选生成**成功之后**才建: 失败零残留, 而受管块生效时输入文件仍然齐备。

**为什么 CI 全绿却没发现**: 所有 E2E 的"现网配置"都是拿模板渲染出来的, 锚点必然存在, 连
"从上一个发布 tag 升级"那一支也是。仓库里**没有任何夹具代表存量机器的配置形态** —— 老模板
加一串迁移堆出来的那种。这一版补上了: 新增判据从模板做等价变换(去掉全部整行注释、结构一行
不动)造出存量形态, 直接盯"受管块装不装得上、位置对不对、真缺结构时是否仍然 fail-closed、
失败有没有留残留"。

**已经在 v1.11.0 上的机器**(即 2026-07-20 之后全新装的)不受这个问题影响, 升到 v1.11.1
也没有行为变化 —— 除了上面那处残留不再产生。

## 2026-08-27 — v1.11.0(可选的 DNS 去广告: 默认关着, 开起来也拦不住自己)

**这一版加的是新功能, 默认关闭。**不想用的人升完不需要做任何事 —— 不启用时解析行为与不装
这个功能时逐字节一样, 配置格式也没有变化。

**在 DNS 这一层把广告与追踪域名答成 NXDOMAIN。**UDP 53、TCP 53、DoT 853 三个入口走的是
**同一条判断链**(`main_sequence`), 不存在"换个协议就绕过去"。判断排在缓存命中终止点
**之前** —— 排在其后的话, 一条被缓存的应答就能绕开整套规则。**不记录任何查询域名**:
这是刻意不做的功能, 不是还没排期。

**优先级靠合取表达, 不靠规则先后。**从高到低:

1. **基础设施强制放行** —— 证书续期要用的 DNS API 域名, 谁都压不过它;
2. **用户 allow**;
3. **用户 block**;
4. **用户显式分流**(`explicit_proxy`, 你自己点名要走出口的域名);
5. **第三方广告表**;
6. 既有的 WDA / geosite / default。

第三方表在**用户规则与显式分流之下** —— 一张几万条的自动表不该悄悄改写你亲手写的那几条。

**第三方表用 last-known-good, 更新失败绝不切成空表。**默认源是 anti-AD(MIT, 纯域名格式)。
下载与编译的每一步失败都**不覆盖上一份可用的表**: fail-open 在这里等于"更新一次就全放行",
比不更新糟得多。下载器只走 HTTPS、精确主机名、443 端口、**零重定向**, 拒绝非公网地址, 且
**解析一次就连那个被校验过的地址**(防 DNS rebinding), TLS 用原始主机名校验证书, 不继承
环境里的 proxy。`enable` 与 `update` 失败时不会留下"启用位写了但规则没生效"的半套状态。

**老机器升级时会先补齐明确代理层, 再装去广告受管块。**受管块里引用了 `explicit_proxy`
这个 tag, 而它由另一支迁移安装。顺序反了的话, 一台还没有明确代理层的老机器会被插进一个
引用不存在插件的块 —— mosdns 起不来, 整次更新回滚, 这台机器从此再也升不上去。现在
`migrate_adblock` 排在它之后, 并且**自己确认那个 tag 真的定义了**(前一支允许自己跳过),
缺前置就跳过安装而不是插坏块; 跳过是可恢复的, 等明确代理层到位, 下一次 `pdg update` 会补上。

**证书续期的基础设施优先放行, 认不全就拒绝启用。**启用前会枚举当前 ACME provider 的 API
域名并写进强制放行名单 —— 这些域名一旦被第三方表挡掉, 证书续期会从此**静默失败**, 几十天
后所有面板同时过期, 期间没有任何告警。本版本明确支持 **5 个 provider**;认不出、脚本缺失或
端点漂移时**拒绝启用**(fail-closed), 并说清是哪一类不认识、可以怎么做。没有 `--force`,
也没有环境变量后门 —— 自己往 allow 里加一条**不算**产品认全了那个 provider。

**CLI 与 Telegram 两条入口。**

- `pdg adblock enable | disable | update | status | check <域名>`;
- `pdg adblock rule-add <域名>` / `rule-del <域名>` —— 事务式增删用户阻断规则;
- Telegram Bot 走**现有的内联按钮**: 主菜单多一个「🛡 去广告」, 二级菜单可以看状态、加规则、
  删规则、查域名。**没有新增 `/adblock_*` 之类的菜单命令**, BotFather 命令表一个字没动;
- Bot 仍然只认现有的 allowed-ID 限制, 授权判定发生在建立任何待输入状态、读文件、取锁、
  起子进程**之前**;
- 待输入操作**绑定发起者**: 群里点了"添加规则"之后, 只有点的人发的下一条才算数;
- **Bot 一个字节都不写规则文件**, 只经固定参数数组调用上面那两条可信 CLI, 并且只认它们返回
  的闭集结果码, 不去解析给人看的文案。

**添加与删除的语义写死。**添加固定写成 `domain:<规范化域名>` 一种形态;删除**只删规范化后
完全相等的那一行** —— 你手写的 `full:` 规则、裸域名、注释、父域、子域、含相同字符串的其它
行一律不动。重复添加、删除不存在的规则都是幂等空操作。

**并发与只读语义。**本版本起, 五个会改状态的入口 —— `enable`、`disable`、`update`、
`rule-add`、`rule-del` —— **共享同一把全局锁**, 与 `pdg update` 互斥, 并且都在第一个写动作
**之前**取锁: 锁被占着时它们一个字节都不会动。规则命令此时返回一个明确的"忙"结果, Telegram
上直接说**「另一个 PDG 操作正在进行, 本次没有修改任何规则, 请稍后重试」**, 而不是含糊地报
失败或回滚。`status` 现在是**严格只读**的 —— 此前它会顺手建出空的状态文件, 于是"这台机器
用没用过去广告"这个问题被工具自己搅浑了; `check` 一直是只读的, 没有变化。

规则改动只在**编译产物真的变化时**才重启一次 mosdns: 幂等添加、删除不存在的规则、产物没变
的情况一律**不重启**。`enable` / `disable` / `update` 的正常路径重启行为**与上一版一致**,
这次没有改动它们。

**要不要用, 完全是你的选择。**升级本身不会启用它;不需要的人无需任何操作。想用的话:

```bash
sudo pdg adblock update     # 取一份第三方表
sudo pdg adblock enable     # 启用
```

三份数据各管各的: **用户规则**(`/etc/mosdns/rules/adblock_{allow,block}.txt`)是你的数据,
进版本快照; **第三方表**可以随时重下, 不进快照; **LKG** 是上一份确认可用的第三方表, 只在
更新失败时兜底。停用不会删掉任何一份 —— 关掉只是让编译产物变空, 随时可以再打开。

**已知边界**(如实登记, 这一版没有做):

- anti-AD 目前是**单一默认源**, 没有自动回退;该源不可用时更新失败(现网靠 LKG 保住)。
- provider 目前只支持 **5 个**;表外的一律 fail-closed 拒绝启用, 不是自动放行。
- 更新完整的第三方表**仍然会重启一次 mosdns**, 期间有短暂解析中断。上面说的"不重启"只适用
  于 `rule-add` / `rule-del` 这两条规则命令。
- 低内存模式(`PDG_LOWMEM`)只缩小 **mosdns 的 DNS 缓存**与 journald 上限, **不会缩小去广告
  的域名集** —— 那两件事不相干, 别指望开了它去广告就更省内存。
- 用户自己的 block 文件目前**没有体积硬上限**(第三方表有 8 MiB 硬顶)。正常是几十到几百行;
  塞进一个极大的文件会额外占内存, 当前没有判据拦它。
- 是否把阻断改到 mihomo 规则引擎 —— **尚未裁决**, 不属于这一版。

## 2026-08-25 — v1.10.16(回滚把面板表带回去了, 反代却还按旧的转发)

**用了内网面板的必升;没用的这一版不影响你。**配置格式没有任何变化, 升完不需要做任何事。

**问题出在"回滚"这个动作本身没做完。**面板表(`lan-panels.json`)和启用意图
(`profile.env` 里的 `PDG_LAN_ENABLED`)都在版本快照里, `pdg update` 失败回滚时它们会
被带回去。而反代真正读的那三个文件 —— `/etc/pdg-lan/caddy.conf`、
`/etc/nftables-pdg-lan.conf`、`pdg-lan.service` —— 都在快照之外, 回滚一个字节都不碰。

于是回滚之后, 面板表是旧的、反代配置是新的, 两边各说各的:

- 出站白名单那半有人对账 —— doctor 会判红, 你看得见;
- **反代那半一直没人看** —— Caddy 继续为面板表里**已经不存在**的面板转发, 全程没有任何
  告警。删过面板的话尤其难查: 现场报的是"白名单多出一条", 而真正的原因在反代配置上。

现在回滚在恢复完面板表与启用意图之后, 会**按恢复出来的那份**重新生成这三个文件并让
`pdg-lan` 跟上: 意图是启用就重渲加重启, 意图是停用就按既有的停用路径收敛, 从来没启用过
的机器则原样不动(那种机器上写一个快照里没有的键, 同样是在改写刚恢复出来的状态)。
收敛做不完时会明确打印**「LAN 回滚不完整」**并指出第一失败点, 整次回滚返回失败 ——
不再报成"已回滚"。

**顺带修掉三个"说成功、其实没成"。**

`_lan_render` 原来是三个文件各写各的, 而它最后一条命令是 `daemon-reload … || true` ——
**恒为真**。于是 unit 生成失败、白名单落盘失败, 整个函数照样返回 0, 调用方接着打印
"已生效"。生成 unit 用的还是重定向, 那会**先把正式文件截断**再执行, 生成失败就留下一个
空 unit。另外反代配置装不上正确属组时会**静默降级**成人人可读。

现在这三个文件是一笔局部事务: 一起生成到临时目录, 反代配置交给真 Caddy 校验、防火墙候选
交给 `nft -c` 校验, 通过了才成组落盘; 任何一步不成立就整笔退回本次操作前的样子并返回失败。
属组装不上就是失败, 没有静默降级这条退路。

另外三处"生成完却没重启反代"的失败以前被 `|| true` 吞掉, 现在会如实往上报, 也不再打印
虚假的"已生效" —— "磁盘对了、进程没跟上"是这个项目反复出事的形态。

**新增自检: 面板表与反代路由的双向对账。**反代多出面板表里没有的站点(仍在服务一个已经
被移除的面板)、少了面板表里有的站点(那个面板打不开, 而白名单是按表放行的, 现场看着一切
正常)、或者上游对不上, 都会判红并点名。期望值用的是**真的那个生成器**现渲一份来比, 不另
写一套宽松的判据 —— 那种第二实现迟早比产品松, 而松的方向就是假绿。读不出配置时明说本项
没跑成, 不判红: 把"没跑成"说成"不一致", 会让一台缺文件的机器看着像配置丢了。

**边界没有变**: `dns.env`(DNS 服务商凭据)、面板证书与私钥、acme 账户密钥、
`/var/lib/pdg-lan`(反代运行态)以及 Caddy 二进制**仍然不进版本快照**。它们是持久凭据与
运行态, 不随面板表变化; 回滚是把现场退回去, 不是拿凭据重建一遍。这一版的收敛过程不读、
不写、也不备份它们。

## 2026-08-25 — v1.10.15(自检的端口证据: 不知道就说不知道)

**这一版只动判据, 不动功能。**DNS、代理、防火墙、Tailscale 的行为一个字节都没变;
真出现行为变化, 那是 bug, 不是预期。

**① 「Tailscale 直连端口对账」原来会把"没有证据"说成"没有问题"。**这条判据(v1.10.14
新增)要比的是防火墙放行的那个 UDP 端口, 和 `/etc/default/tailscaled` 里声明的端口。
可是读不到那份文件、或者文件里没有 `PORT=` 时, 它判的是**绿**。绿的意思是"对上了",
而实情是"这台机器上我们根本不知道对不对得上"。现在这两种情形一律判**警告并明说本项无
结论**, 并顺带说清没装 Tailscale、或不是 deb 装的属于正常情形, 不必处理。

判警告而不是判失败是有意的: 失败会让 `pdg update` 的自检门整次回滚, 而"这台机器没装
Tailscale"根本不该阻断更新。这一项也不影响 `pdg doctor` 的退出码。

**② `PORT=` 的取值改按 systemd 的规矩来。**那份文件是 unit 的 EnvironmentFile, 写了多条
赋值时**以最后一条为准**。原来取的是第一条 —— 正好取到被覆盖掉的那个值, 而且错得很安静:
两边"对上了", 实际用的却是另一个端口。现在逐行解析、跳过注释、后面的覆盖前面的; 最后一条
解析不出合法端口时**不退回前一个值**, 直接判无结论 —— 拿一个已经被覆盖的旧值冒充现状,
正是这条判据要防的事。

文案也改了: 不再把 `/etc/default/tailscaled` 说成"实际监听端口"。这是**两份配置的对账**,
不是监听探测 —— 进程有没有按它跑, 这条判据管不着。

**③ functional 测试显式建立自己的前提(测试可靠性, 与现网行为无关)。**这支测试要靠
conntrack 才能拿到被重定向连接的原始目的地, 而它一直靠运行环境碰巧提供; 碰不上时三个出口
一条日志都没有, 表面症状是"分流没生效", 真因看不出来。现在它会先探一次, 缺了就自己建一张
本次运行独占的临时表, 用完删掉; 环境不允许则**具名硬失败**而不是悄悄跳过 —— 跳过等于零
覆盖, 而零覆盖会以绿灯的样子出现。

> 这一节是**事后追补的仓库文档**: v1.10.15 已经正式发布, tag 与 Release 都不动。
> 当时按要求没有随版本提 CHANGELOG, 发布说明只存在于 Release 页面。
>
> 另外三件常被一起提起的判据诚实性修复**不属于这一版**, 别记错了账:
> 「tailnet ACL 越界探测不再出具安全证明」「`pdg link status` 的 DoT 文案与真实证据一致」
> 「`tailscale down` / 未启动不再被当成卸载残留」三条都在 **v1.10.12**;
> 「Tailscale 直连端口对账」这条判据本身是 **v1.10.14** 新增的。

## 2026-08-24 — v1.10.14(证书续期是哑的; 上一版的修复自己装不上)

**用了内网面板的必升。**这一版修的是三个"磁盘对了、进程没跟上"的问题, 其中一个有明确
的爆炸日期。

**① 证书自动续期一直没生效, 到期那天所有面板一起报错。**

acme 装完新证书之后会跑一条重载命令, 而那条命令写的是
`systemctl reload pdg-lan 2>/dev/null || true` —— 那个 `|| true` 把失败吞成了成功。
它**必然失败**: 反代配置里是 `admin off`(刻意的, 不该为了重载去开管理端口), 而重载走的
正是管理接口。

平时看不出来, 是因为你每次加/删面板都会顺手重启一次。**只有"纯续期、没人碰面板"这条
路径中招 —— 而那正是它无人值守跑的常态**: 新证书落盘, 反代内存里还是旧的, 一直用到
旧证书过期那天, 所有面板同时报证书错误, 而且全程没有任何告警。按当前证书算, 下一次
续期在 10 月下旬。

**② `pdg lan render` 只生成不重启。**同一个毛病的另一处。真机上撞到过: 配置文件是新的、
反代还停在九小时前, 新规则没进内存。

两处都收到一个入口上, 而且必须是重启不是重载 —— 出站白名单是靠服务启动时加载进内核的,
重载根本不跑那一步。

**③ 上一版(v1.10.13)修的跳转头问题, 存量机器自己装不上。**

`pdg update` 只刷新代码, 不碰已经生成好的反代配置。于是生成器修好了、机器上还用着旧规则,
**而且所有自检都是绿的** —— 面板表、白名单、证书全对, 错的只是反代里的一条改写规则。
现在升级时会自动认出旧配置并重新生成。

**④ 那条改写规则本身也还漏着三种情况。**上一版要求域名后面必须跟 `/`, 而
`http://面板域名:端口` 这种不带路径的、直接跟 `?` 或 `#` 的, 都是合法写法, 全漏了。
已补齐。

顺带新增两条判据:

- **反代重载的唯一入口**有守卫盯着, 以后再写出 `reload` 会当场判红;
- **Location 改写改用真 Caddy 打真响应头来验**。原来只比对生成出来的文本, 而上面那三种
  漏网形态在文本比对下一路绿灯 —— "字符串长这样"和"响应头真的被改了"是两回事。

**⑤ 新增自检: Tailscale 直连端口对账。**

`pdg ssh-source tailnet` 会放行 UDP 41641, 那是 Tailscale 的默认端口, 但**它是可配的**。
你要是改过 `/etc/default/tailscaled` 里的 `PORT=`, 这条放行就静默地两头落空: 41641 那条
没有监听者, 而真正在用的端口被防火墙挡着。后果是 `pdg ssh-source` 本来要消除的冷启动
窗口又回来了 —— 空闲一段之后第一次 SSH 会超时, 而那正是最需要它的时刻。

现在 doctor 会把两边对一遍, 不一致就报, 并告诉你该改哪个。判据只读配置不发探测包:
端口通不通要发包才知道, 而"探不到"证明不了任何事。

## 2026-08-24 — v1.10.13(内网面板: 上游把自己的端口写进跳转头)

**用了内网面板、而且上游跑在非标准端口上, 才需要升。**其它人这一版没影响。

**症状是浏览器卡死, 而不是报错。**访问一个目录地址(结尾不带斜杠)时, 上游会回一个 301
让你补上斜杠 —— 而它拼这个跳转地址时, 会把 `Host` 头原样回显、再补上**自己实际监听的
端口**:

```
http://nas.example.com:30035/dir/
```

主机名是对的, 错的是 scheme 和端口。反代只在 443 上, 客户端跟着跳过去就到不了, 浏览器
显示「服务器已停止响应」, 一直转圈。

这不是个别设备的毛病。`UseCanonicalName Off` 是 Apache 的默认行为, 所以**凡是跑在非标准
端口、又遵从 Host 头的上游, 都会这样**。真机上是 TrueNAS 的 WebDAV(Apache 2.4, 端口
30035)撞出来的。

**为什么 `--rewrite-location` 接不住。**那条规则锚的是**上游的局域网 IP** —— 家用设备
把自己的 IP 写进跳转头(Zyxel 交换机、华为 UPS 实测), 它正好接住。而这里上游写的是**已经
正确的主机名**加**错误的端口**, 正则匹配不到。

现在无条件再生成一条, 把指向本域名、却带着别的端口或 scheme 的跳转一律拉回来。**不需要
你加任何参数** —— 这种跳转对客户端永远是坏的, 不存在「用户可能想要」的情形。加过的面板
执行一次 `pdg lan add`/`rm` 或 `pdg lan enable` 就会重新生成。

值得一提的是这个 bug 的来路: 服务端怎么测都测不出来。curl 命令习惯性带结尾斜杠, 永远
触发不了那个跳转 —— **是真人用浏览器点出来的**, 而且是靠盯着地址栏里多出来的端口号。

## 2026-08-23 — v1.10.12(自检里三条会骗人的绿灯)

**这一版不改功能, 只修判据。**改的是 `pdg doctor` 和 `pdg link status` 说的话 —— 它们
有三个地方会给出比证据更强的结论。这类错误比报错危险: 报错有人查, 假绿没人查。

**① 「tailnet ACL 边界」这一项证明不了它声称的东西。**`pdg doctor --deep` 会从网关往一个
不在面板表里的地址试着连, 连不上就判绿, 文案写「ACL 边界成立」。问题是那个地址是**猜**
出来的 —— 它上面本来就可能没有设备。地址不存在时, 你的 tailnet 是默认全通还是已经收紧,
观测**一模一样**。那个绿灯只证明了「这个猜出来的地址没有回应」, 与 Access controls 配没配
毫无关系。

现在改成: 连得上、或者收到 RST, 说明包**到达了对端**, 那是确凿的越界 —— 仍然判失败。
探不到则明说**无结论**, 并告诉你差的是什么(一个已知存活、又在面板表之外的对照地址, 用来
先把仪器校准)。所以这一项从今往后只帮你发现越界, **不出具安全证明** —— 那件事只能你自己
去 tailnet 后台核对。如果你之前看着这条绿灯以为 ACL 已经配好了, 请去看一眼。

**② `pdg link status` 会一边给你 DoT 证据, 一边说自己看不到 DoT。**手机链路测试真的观察到
DoT 查询时, 同一屏先打出 🟢 手机 DoT 查询证据, 紧接着又来一句「本版本无法观察手机是否真的
发出了 DoT 查询」。那句话在 `pdg-dotwitness` 落地之前是对的, 之后就成了自相矛盾, 而且会
把上面那条**真证据**抹掉。现在结尾那段说明按本次真拿到的证据逐条拼: 拿到 DoT 就说到 DoT,
没拿到就直说没拿到。能说的上限没变 —— 每条证据只证明它自己那一次到达。

**③ 「Tailscale 卸载残留」会把 `tailscale down` 当成卸载, 还让你删包里的文件。**它原先只
凭 `tailscale0` 接口不在就断定 Tailscale 被卸了。可接口不在的原因还有 `tailscale down`、
tailscaled 临时停、以及装好了从没 `tailscale up` 过。那三种情形下 `src_valid_mark=1` 本来
就正常, `/usr/bin/tailscale` 也是包自己的文件 —— 照原判据会建议你 `rm -f` 掉一个 **dpkg
拥有的文件**, 把包弄成破损状态, 而且要到下次 `apt` 操作才看得出来。现在先看包还在不在
(问 dpkg、看 tailscaled 的 unit 文件, 两者都与"此刻通不通"无关), 包还在就整项不适用。

**README 补了不少 Tailscale 的实操。**起法分「只做管理通道」和「要用内网面板」两种(差别
就一个 `--accept-routes`), `--accept-dns=false` 为什么是必须的, 以及默认不要开的几样(出口
节点、路由宣告、Tailscale SSH、Serve/Funnel)。新增 Access controls 一节 —— **新建的 tailnet
默认是全通的**, 网关一旦接受了家里的子网路由, 这句话就等于"被拿下的网关能摸到你家整个
内网"; 给了 Grants 写法(老的 `acls` 标成 legacy 只作对照)和 tag 怎么才能真的生效。还补了
三条容易踩的边界: 节点密钥默认 180 天到期(到期就掉出 tailnet, 面板全断, SSH 若已收紧成
只走 tailnet 就直接进不去)、Android 的「私密 DNS」跟 Tailscale 是两回事、「手机零 App」
只覆盖网关反代出来的那几个面板。以及: **Tailscale 客户端的版本要你自己跟进**, `pdg update`
不碰它。

顺带删掉一句说过头的话: 旧文档写 `nodivert` 模式下「Tailscale 自己的反欺骗保护照样生效」。
本项目实测到的其实是 `inet pdg` 的入口隔离加默认丢弃挡下了伪造源地址的包 —— 那证明不了
`ts-input` 还在工作(nodivert 下它根本不被执行)。要那一层请以官方文档为准。

## 2026-08-22 — v1.10.11(内网面板: 从 Telegram 点进去的面板打不开)

**用了内网面板就该升。**没用的话这一版只改了 README。

**从按钮点进去的面板可能打不开。**有些设备会校验 `Referer` 必须是它自己的地址(华为
UPS2000 实测)。上一版的改写只处理"指向面板域名"的那种 Referer —— 而从 Telegram 按钮点
进来时, 浏览器带的是 `t.me` 或 `android-app://…`, 规则匹配不上, 外来 Referer 就原样传给
了设备, 设备回一句 `Error Referer Request!`。

现在的判据是"**有没有** Referer"而不是"它指向谁": 没有就不加(浏览器首次导航本来就不带,
凭空塞一个进去等于伪造来源, 严格校验的设备照样拒), 有就不管指向谁一律换成上游地址。

值得一提的是这个洞的来路: 它一直都在, 只是以前手输网址不带 Referer, 十几个月都没碰到。
**是新加的直达按钮把它照出来的。**

**重跑 `pdg lan cert` 会看到一句假的失败。**acme.sh 在"证书还有效、这次不用续"时也返回
非 0, 而旧代码把任何非 0 当失败 —— 于是不但报了错, 还跳过了把证书装到位那一步。现在改成
看证书库里有没有一张没过期的证书, 有就继续装。

**Telegram 里新增: 证书还没覆盖到的面板, 按钮上直接标「待重签」。**加了面板之后按钮会
立刻出现(每次点开都现读面板表), 但所有面板共用一张证书, 不重签的话新面板在手机上是证书
错误 —— 而按钮长得和能用的一模一样。现在不用点下去才发现。

**README**: Tailscale 那一节重写得读起来自然些; 新增「内网面板」一节 —— 这个功能发了
三个版本, README 里一直一个字都没有。顺带补上 `pdg ssh-source` 与 `pdg lan` 两条命令
(都是发布过但文档没跟上的)。

## 2026-08-22 — v1.10.10(内网面板: 升级路径修复 + 一个安全洞 + Telegram 里点开面板)

**如果你在 v1.10.7/v1.10.8/v1.10.9 上启用过内网面板, 这一版是必须升的。**没用这个功能
的话, 下面只有最后一段与你有关。

**① 之前你可能根本升不上来。**`pdg update` 跑完自检, 而证书布局在 v1.10.8 变了
(一板一张 → 所有面板共用一张 SAN 证书), 旧布局的机器这一项判"失败"→ **整次更新回滚**,
每次都这样。现在: 证书相关的自检一律只报警告不报失败 —— 面板打不开是问题, 但不该把一个
修了 DNS/代理缺陷的更新一起挡掉; 而且会**自动把旧证书搬到新位置**(旧版那张本来就覆盖了
全部面板, 只是装错了地方), 不用重签。

**② 每次防火墙重建之后, 反代都在没有出站白名单的状态下跑。**`/etc/nftables.conf` 开头是
`flush ruleset`, 它清掉**整个**防火墙规则集, 内网面板的白名单一起没; 而反代不会因此重启。
那一刻它能连到你家内网的任意地址 —— **这是安全洞, 不是不便**。全项目重新加载防火墙的地方
有九处, 现在收成一个入口, 加载完就把白名单补回去; 另加一条守卫拦住将来新写的绕过。

**③ 加面板之后不用再自己想着做什么了。**`pdg lan add` / `rm` 现在自己把四份派生物跟上
(反代配置、出站白名单、DNS 劫持集、分流规则), 并当场告诉你证书里还缺哪些面板 ——
以前它只说一句"已加入面板表", 而实际上还得再跑两条命令, 不跑的话新面板打不开。

**④ Telegram 里能直接点开面板了。**「运维 → 🏠 内网面板 → 🔗 打开面板」列出所有面板的
按钮, 点一下直接开浏览器, 不用手输一长串地址。同一处还能看列表、看状态、加面板、删面板、
重新生成。这个入口只在**配过内网面板**的机器上出现。

注意手机要走**蜂窝网络**: 连着 WiFi 时 DNS 查询不是从内网卡过来的, 网关不会劫持这些域名,
点了会打不开。按钮页上也写了这一条。

**没用内网面板的人**: 这一版对你唯一的变化是防火墙重新加载走了统一入口 —— 行为不变,
`pdg update` 前后的规则逐条相同。沙盒里跑了全新安装(两平台)、从 v1.10.6 升级、卸载三个
场景, 以及"在 v1.10.7 启用过面板再升上来"这条最容易出事的路径。

## 2026-08-22 — v1.10.9(内网面板的两处头改写: 照搬了意图, 没照搬形式)

**只影响用了内网面板的人。**两处都是手机上真访问才暴露的 —— 从网关自己 curl 一下看不出来。

**入口参数会无限跳转。**给前后端分离的应用(sub-store 之类)在入口带参数那条, 判据只写了
"路径是根" —— 可跳到 `/?参数` 之后**路径仍然是根**, 于是再跳一次, 手机上报"重定向次数
过多"。判据要加上"而且还没带这个参数"。从网关用 curl 不跟跳转只看得到第一个 302, 一切
看着正常。

**Referer 不能凭空塞。**有些设备校验 Referer 必须是自己的地址, 旧实现是**无条件设置**一个
——可浏览器首次导航本来就不带 Referer, 硬塞一个进去等于伪造来源, 严格校验的设备照样拒。
华为 UPS2000 实测: 根路径不带 Referer 回 302, 带任何 Referer 都回 404(带不带端口没区别)。
改成只把**已经存在的**那个头里的对外域名换回上游地址。

**跳转头改写换成一条正则**覆盖 http/https 与带不带端口, 而不是几条字面替换 —— 逐条穷举
总会漏一种, 漏掉的那种表现是手机跳到一个到不了的地址。

这两处的共同来路值得记下来: 它们的正确写法本来就在作者自己那套跑了很久的配置里, 而移植
时搬过来的是"要改哪几个头", 没搬"怎么改" —— 后者才是在真设备上踩出来的部分。

## 2026-08-22 — v1.10.8(修 v1.10.7 的内网面板证书: 7 个面板只装上 1 个)

**只影响用了内网面板的人。**没启用的话这一版对你没有任何变化。

**如果你在 v1.10.7 上跑过 `pdg lan cert`, 那次多半是坏的 —— 而它报的是成功。**
升上来之后重跑一次 `sudo pdg lan cert <dns插件名>` 即可。

两个 bug 都是在真机上七个面板的场景下才暴露的:

**证书装错了。**acme.sh 一次签发多个域名产出的是**一张** SAN 证书(覆盖全部名字),
存放在第一个域名的目录下。旧实现却按域名逐个去装, 于是除第一个之外**全部失败** ——
七个面板只装上一个, 而命令最后仍然打印"证书已就位"。现在装一次共用证书, 反代配置里
所有站点指同一对文件。

**证书属主静默错了。**把证书交给反代用户那一步带着"失败也继续", 而那个用户是
`pdg lan enable` 才创建的 —— 按文档顺序 `cert` 跑在它前面, 于是那一步默默失败, 证书
留成只有 root 能读。反代不是 root, **读不到私钥**: 症状是服务起不来, 而 `cert` 那边
报"已就位", 两处输出都不指向真正的原因。现在 `cert` 自己把用户建出来, 并且这一步
失败就当场报错停下。

**顺带补了一条新判据。**共用一张证书带来一个新的出错方式: 加了面板却没重签 —— 证书
文件在、也没过期, 但新面板的名字不在证书的 SAN 里, 手机上表现成证书错误。只看
"证书在不在"会把这一整类漏掉。现在 `pdg lan enable` 的前置检查与 `pdg doctor` 的证书项
都会核对 SAN 是否覆盖了**每一个**面板, 缺哪个点哪个。

代价一并写进代码注释: 加一个面板要重签整张证书, 而且**一个名字签不下来会连累全部**。

## 2026-08-22 — v1.10.7(内网面板访问: 手机零 App 打开家里的 Web 面板)

**这一版加了一个可选功能, 不开就完全不影响你。**升上来之后一切照旧 —— 面板要自己
`sudo pdg lan enable` 才会启用, 而启用之前连 Caddy 都不会装。

**能做什么。**在外面用手机打开家里的路由器后台、NAS、UPS、交换机这些 Web 面板,
**手机上不装也不开任何 App**。链路是 手机 →(SIM)→ 网关 → tailnet → 家里的设备;
手机侧一个字都不用改, 它只是在访问一个普通的 HTTPS 网站。前提是家里有一台常开的
机器能跑 Tailscale 子网路由(OpenWrt / 群晖 / 树莓派都行)。

设计与三道门见 `docs/design-lan-panels.md`。

**三道门都在代码里, 不是文档里的建议。**

第一道挡的是**唯一的灾难级**: 家里通告的网段一旦与网关正在用的段相交, 手机的分流会
直接错乱, 而 nft / mosdns / mihomo 三份配置**一个字都没变** —— 只是包不走原来那条路了。
这种故障从配置上完全看不出来, 所以判据必须在接受之前跑, 拒绝时点名是哪个网段与什么
冲突。`pdg lan routes <网段>` 可以先问一句。

第二道要求域名到 `IP:端口` 一对一, 不接受通配, 而且**上游必须写字面 IP** —— 写域名的话
决定网关去连哪台机器的是 DNS, 而这台网关自己就在做 DNS 劫持。

第三道是反代进程的出站白名单, 按运行它的 uid 过滤而不是按端口(端口挡不住别的进程发起
同样的连接)。除了面板表里出现过的地址, 它什么都连不到。

**风险也在代码里说, 不只写在文档里。**如果面板域名与本项目的 DoT 域名在同一个 DNS zone,
签面板证书要的那个 API token **也能签发你的 DoT 域名** —— 一台被拿下的网关可以用它
反过来 MITM 你自己的 DNS。这是权限升级, 不只是多一个凭据。`pdg lan status` 每次都会
提醒, 并给出收窄办法: 面板用单独子域, `_acme-challenge` 用 CNAME 委派到单独 zone,
然后 `pdg lan cert <dns插件名> <委派zone>`。

**doctor 多了四项**: 子网路由重叠(与功能开没开无关 —— 危险来自"接受了重叠路由"这件事
本身)、出站白名单与面板表是否逐条一致、面板证书剩余天数、以及 `--deep` 下的 tailnet ACL
越界探测(真正的硬边界在家里那台上执行, 网关被拿下也改不了别人机器上的过滤器 ——
这一条是能测出来的, 不是只能靠嘱咐)。

**卸载会把凭据带走。**DNS API token 与 acme 账户密钥都删, 并明说"证书续期从此不再进行";
删不掉的会逐条点名。`pdg lan purge [--keep-table]` 是不卸载整个项目时的清理入口。

**如实说明**: 沙盒里验的是"产物正确"(配置内容、规则次序、白名单与面板表逐条一致),
**不是"手机真的打开了面板"** —— 后者要真实的 tailnet 子网路由与真实设备, 沙盒造不出来。
acme.sh 的真实签发与 60 天后的续期同样没验过(doctor 的证书项就是为这条不确定性存在的)。
完整清单在 `docs/design-lan-panels.md` 第 9 节。

不用这个功能的话, mihomo 配置的渲染结果**逐字节不变**。

## 2026-08-21 — v1.10.6(SSH 可以收紧成只能经 Tailscale 登录)

**新增 `pdg ssh-source`。**把 22 端口从"对全网开放"收紧为"只接受从 tailnet 进来的连接"。
公网扫描器每天敲你 22 端口那件事就此消失 —— 不是改端口或 fail2ban 那种减速带, 是那些包
根本进不了 input 链。

这是**能把自己锁在门外**的操作, 所以两道门都做得比较重:

- **前置门:必须此刻正通过 tailnet 连着。**判据不是"装了 Tailscale", 而是去 `ss` 里看
  当前这条 22 端口会话的对端地址在不在 `tailscale status` 里。装着 Tailscale 不等于这条路
  通得了(空闲后打洞要重来), 而收紧之后判错的代价是只能去开服务商的网页控制台。
- **自动回退:10 分钟内不确认就自己还原。**改完要**另开一条** tailnet 会话验证能进来,
  进得来再 `pdg ssh-source confirm`;进不来就什么都不做, 等它自己退回去。时限可用
  `PDG_SSH_REVERT_MIN` 调。`systemd-run` 起不来时会明说"现在没有任何兜底", 不假装有。

**收紧的同时会放行 UDP 41641。**这是同一个决定的两半:不放行的话 Tailscale 打不了直连、
只能走 DERP 中继, 表现是空闲一段之后第一次 SSH 卡顿甚至超时 —— 而那时它已经是你唯一的
登录路径了。恢复 `any` 时一并撤销。

**这条限制活得过模板重建。**以前 `nft` 模板一重建就把来源限制冲掉、22 端口静默地重新对
全世界开放。现在模板里 SSH 那行是按当前配置反推渲染的, `pdg update`、平台切换、模板同步
都不会冲掉它;认不出当前形态时**拒绝改动**, 而不是按默认值重渲染。

其余:README 补了 Tailscale 一节(它与本项目防火墙的交界处、`NetfilterMode` 那条硬约束、
卸载后会留下什么);新增设计文档 `docs/design-lan-panels.md` —— 手机零 App 访问家里内网
面板的两种架构对比, **只是设计, 本版没有实现**。

**如实说明**:两台线上机**没有**实际收紧过 —— 那是你自己的决定, 不是升级会替你做的事。
升上去之后 SSH 行为不变, 要收紧得自己敲 `pdg ssh-source tailnet`。

发版前在沙盒里跑了全新安装(两平台)、从 v1.10.5 升级、卸载三个场景, 都过;卸载后防火墙
三条链都是 `policy accept`, 不会把人关在门外。"来源限制活得过模板重建"这条另外做了**正负控
对照**:同一份收紧过的配置, 在 v1.10.6 上强制重建后原样保留, 在 v1.10.5 上被还原成
`tcp dport { 22 } accept` 且 41641 一并消失 —— 静默的, 没有任何提示。CI 29 项全绿, 其中
新增 18 条断言专盯这一件事。

## 2026-08-20 — v1.10.5(一批积压的内部缺陷;四处会改变你敲命令后的观感)

这一版没有新功能,是把交接文档里挂了很久的一批 P2/P3 一次清完。多数改动你看不见,
下面四条会。

**`pdg update` 已经是最新版时不再空跑一遍。**以前无论如何都会走完全程:多留一份快照、
重启 pdg-bot、把所有已装文件的 mtime 刷新一遍。用户数据不受影响,但事后想按 mtime 查
"这次更新到底改了什么",得到的是全部文件。现在会直接告诉你已是最新并退出。
判据要求**仓库在最新 tag 上、工作树干净、且已装文件逐个与仓库一致**——只比版本号不够,
盘上完全可能躺着旧的或被手改坏的文件,而那正是 `pdg update` 要修的。任何一项读不出来
就照常更新(宁可白跑一次,也不能让修复命令变成空操作)。要强制重装同一版本:
`PDG_UPDATE_FORCE=1 pdg update`。

**`pdg rollback` 现在会把代码一起带回去。**以前手动回滚只还原配置文件,仓库还停在新版本
——于是 `pdg version` 和 doctor 报的是新版号,机器上跑的却是旧代码,两边说法不一致而且
没有任何一处会提。现在默认按快照自己记下的提交复位仓库,动手前会先说一声。只想回配置、
代码留在新版:`pdg rollback --no-git`。老快照没记提交时会明说,不会闷头跳过。

**doctor 报「防火墙链冲突」时会指出可能是谁建的。**这条判据以前只说"有冲突",定位得
从头做。卡点在于 `nft list` **显示不出 iptables 规则的 comment**——同一条规则在 nft 眼里
只是一句普通放行,换 `iptables -S` 一看归属就写在上面。现在 doctor 会用 iptables 的视角
再看一遍,把 comment 原样列出来,认得出的来源直接点名(证书钩子 / Tailscale / Docker /
fail2ban / ufw / libvirt),认不出就只提示换个工具看,不猜。

**doctor 新增「Tailscale 卸载残留」。**Tailscale 会把 `net.ipv4.conf.all.src_valid_mark`
从 0 改成 1 且不落 `/etc/sysctl.d`,卸载后仍是 1,要重启才恢复;`apt purge` 之后
`/usr/bin/tailscale` 也仍留着。两样都是 Tailscale 自己的行为,但只有本项目的 doctor 会去看。
**判据只在卸载之后成立**——机器上还有 `tailscale0` 时这些都是正常状态,不报警。

其余不影响日常使用:锁文件打不开时会带出系统给的真实原因(以前取锁那句会把整个进程的
stderr 重定向掉);事务目录名补到毫秒(同秒的两笔以前只能靠随机后缀区分);
`pdg detect-cidr` 只统计入站包的源地址,装了 Tailscale 的机器上不再被 DERP 中继流量
拽向本机所在段;健康自检定时器回滚前会先清 start-limit。

**如实说明**:这批里没有一条是线上出过事的——它们都是审查时记下来、一直没修的。
发版前在沙盒里跑了全新安装(两平台)、从 v1.10.4 升级、卸载三个场景;
`pdg update` 短路与 `pdg rollback` 的新默认是在真装机上验的,不只是单元测试。

## 2026-08-19 — v1.10.4(证书 standalone 续期钩子:一套从没成功过的机制)

**只有在这台机器上配过「standalone 方式续期的域名」时,这一版才与你有关。**
本项目自己的 DoT / iOS 证书不走这条路径,不受影响。

certbot 用 standalone 方式续期时,需要公网能连到本机 80 口。项目为此提供了一对钩子:
续期前临时开 80,续期后关掉。问题是**这对钩子从来没有真正成功过**。

开 80 的那一步在某些情况下会把放行写进 `table ip filter`。那张表和本项目的
`inet pdg` 挂在同一个入口上,而后者是"默认拒绝"——于是写进去的放行从一开始就被架空,
一个数据包都没匹配过。认证必然失败;而认证失败时 certbot 是非零退出,关 80 的那一步
未必会执行,于是每失败一次就在表里多留一条。

留到两条时,`pdg doctor` 会判红两项(「80 对全网放行」和「防火墙链冲突」),
**升级因此整次回滚**。这是 v1.10.3 在一台机器上装不上去的第二个原因。

这一版把三处都修了:放行只写到本项目自己的表里,写不进去就明确报错而不是留一条没用的
规则;撤销时按我们自己打的标记逐处精确删,不按端口删(你自己写过的同端口放行不会被误伤);
开 80 之前也先清一遍上次留下的残骸,不再把清理只挂在"续期之后"那一步上。

**如实说明**:这套机制此前从没成功过,所以这次修复**没有历史成功样本可以对照**。
两台线上机目前都没有 standalone 续期的域名,修复要等到真有人配这样一个域名时才会
第一次被真正跑到。更彻底的做法是让这类域名改走 webroot 或 DNS-01,根本不需要临时开 80
——那不在这一版的范围里。

## 2026-08-18 — v1.10.3(修 v1.10.2 在部分机器上装不上去)

**如果你在 v1.10.2 上更新失败并被回滚, 这一版就是修那个的。**

v1.10.2 引入了"升级时按模板重建防火墙"。重建前它要先从机器当前的配置里读出几个参数
(SSH 端口、内网段、救援端口), 读不出就放弃重建 —— 这个谨慎本身是对的, 不拿猜来的参数
去改一台正在服务的机器的防火墙。

问题出在读救援端口的方式上: 它只认**当前模板渲染出来的那种写法**。而装机时间较早的机器
用的是更早的模板, 那时救援端口不作为独立规则存在, 只出现在启用救援时写进去的那条规则里,
而那条规则的写法不一样。于是端口读不出来, 重建被跳过, 新版要求的规则一直没装上,
自检发现缺规则, 整次更新回滚 —— 每次都是这样, 重试也没用。

现在两种写法都认得。启用过救援的机器优先按救援规则上的标记来读(那是我们自己写的记号,
不会和你自己配的同端口放行搞混), 没启用过救援的机器则读模板里那条。

顺带把提示改准了: 以前无论"没找到"还是"读出好几个不一样的值", 都笼统说成"不唯一",
排查方向完全被带偏。现在两种情况分开说。

**升级路径**: v1.10.1 或 v1.10.2 的机器直接 `sudo pdg update`。本版只改参数读取与提示,
不改 DNS 配置、服务拓扑和防火墙规则本身。

## 2026-08-18 — v1.10.2(升级时防火墙真的会按模板重建了)

这一版没有新功能。修的是三个互相牵连的问题, 共同点是: 规则明明在, 或者本该在, 而系统
以为不在 —— 或者反过来。

**升级时防火墙终于会按模板重建。** 配置文件顶上一直写着"本表每次更新都会按模板重建",
但那只是句承诺: 早年那次"换表"迁移做完之后, 机器就再也收不到模板的任何改动了, 跑的
永远是装机那天的那一版。平时看不出来, 直到有新的自检项开始检查具体规则在不在 —— 那时
更新会因为"规则缺失"整次回滚, 而缺的那条恰恰是这次更新要装的。现在会重建, 而且分三种
情况: 盘上是旧的就重建; 盘上和内核都已经是新的就什么都不做; **盘上新、内核还跑着旧的
(装机中断、写了没加载、快照只还原了文件)就只重新加载一次, 不动磁盘**, 加载完还要回头
确认内核真的跟上了, 没跟上就明说失败, 不当作成功。

**启用过救援平面的机器不会再被自己的更新切断救援通道。** 救援放行是运行时注入到防火墙
配置里的, 不在模板中; 按模板重建会把它一起抹掉。现在重建时会原样保留。同时自检里有一处
把救援放行误当成"内网来源接管规则", 导致启用救援的机器一升级就判红并回滚 —— 那条放行
指向的是单个地址, 不是接管一整个网段, 现在两者分得开了。

**修正一处会让规则"隐身"的判读缺陷。** 测试环境里有一层把防火墙规则转成结构化数据的
转换器, 它遇到不认识的匹配条件会直接丢弃而不是报错。于是按接口名写的规则在内核里好端端
存在, 自检却看不见它, 判成"规则缺失"。这类静默丢弃最难查, 因为现象总是指向别处。

**升级路径**: v1.10.1 的机器直接 `sudo pdg update`。本版只改自检与升级时的防火墙处理,
不改 DNS 配置与服务拓扑。你写在 `/etc/privdns-gateway/nft-input.d/` 里的自定义规则不受
影响 —— 那个目录重建时不会被碰。

## 2026-08-17 — v1.10.1(两处"一切正常"底下的静默失效)

这一版没有新功能, 修的是两个**平时完全看不出来、出事那天才一起爆发**的问题。共同点是:
每个部件单独看都健康, 自检也全绿, 因为没有任何一项在问那个真正要紧的问题。

**证据端可能被别人顶包。** 上一版的「已知边界」里提过这条, 现在补上了。DoT 证据端启动前
只检查"5399 上有没有人在听", 不问在听的是不是它自己。于是证据端自己起不来、端口还被别的
程序占着时, 迁移照样报"已就绪" —— 链路测试拿到的"看到了", 实际来自一个不相干的进程。
现在网关会确认那个监听者确实归证据端所有, 不是就明确失败并回滚。拿不到判断依据的机器
(比如精简系统上没有相应工具)会如实说"这次没校验", 而不是假装校验过。

**证书续期可能一直在往没人读的目录里写。** 网关读证书的目录和续期钩子写证书的目录, 如果
不是同一个, 中间没有任何一环会报错: 证书文件在、没过期、续期成功、自检全绿。故障被安排在
证书到期那天 —— 所有手机的 DoT 在同一时刻失效, 而在那之前一切正常。`pdg doctor` 新增一项
直接比对这两个目录, 不一致就报错, 并同时告诉你两个路径分别是什么、该动哪一头。

**升级路径**: v1.10.0 的机器直接 `sudo pdg update` 即可。本版只增加检查、不改动 DNS 配置
与服务拓扑。升级后 `pdg doctor` 会多出一项(证书路径一致性), 正常机器仍是全绿。

## 2026-08-15 — v1.10.0(手机的 DoT 查询, 网关这次真的"看见"了)

上一版能告诉你手机的 HTTP 请求有没有到达网关, 但 DoT 那条路仍然是盲的 —— 链路测试只能说
"没看到", 不能说"看到了"。这一版把 DoT 的证据补上了。

**网关新增一个只对本机开放的证据端。** 它只在 `127.0.0.1:5399` 上听, 以动态用户运行,
不能联外网、不能提权, 内存和进程数都有硬上限。手机做一次 DoT 查询, 网关这头就能确认
"这次查询确实走了加密 DNS", 而不是靠猜。

**判定必须两个条件同时成立**: 查询的域名落在探测专用命名空间下, 并且 TLS 的服务器名恰好
等于你的 DoT 域名。明文 DNS 永远满足不了第二条, 所以混不进来。

**结论只有三种, 而且不会含糊**: 看到了(OBSERVED)、没看到(NOT_OBSERVED)、看不了
(UNAVAILABLE)。证据端本身不可用时, 网关会明说"看不了", **绝不会因此说成"看到了"**。

**证据留存 600 秒。** 上一版留存期和会话窗口共用一个数, 证据往往在结算之前就过期, 于是
"没看到"这个结论实际上永远出不来。现在留存期覆盖整个窗口加结算余量, 三种结论都真正可达。

**隐私**: 落盘的只有探测串的哈希, 不存明文、不存完整域名、也不存你的 DoT 域名;
日志里同样不出现这些。证据文件权限 600, 归属运行该服务的动态用户。

**升级路径**: v1.7.8 与 v1.9.0 的机器都可以直接 `sudo pdg update` 升上来。DNS 配置的改动
是外科式的 —— 只在受管标记之间动, 你自己加的段落逐字节保留。任何一步失败都会精确回滚到
更新前的样子: 三个文件的内容、权限、归属, 两个服务的启用与运行状态, 全部按更新前的快照还原;
万一没能完全复原, 网关会明说哪里没还原干净, 不会闷声算成功。

**普通 DNS 不受影响。** 证据端出故障、被停掉、端口被占, 普通解析照常走 —— 这条有独立的
故障隔离测试盯着。

**装机、卸载与自检都已闭环**: 全新装机自带证据端; 卸载不留残骸; `pdg doctor` 单独一项
报告它的状态。

**已知边界(本版**未**修复, 不影响正常使用)**:
- 迁移前若 5399 端口已被其它程序占用, 启动前的检查只看"有没有人在听", 不校验监听者身份
- 测试用探测脚本的缺省域名是占位值, 真实环境下需显式传入本机 DoT 域名
- 测试框架里 YAML 的语法门是空的, YAML 有效性靠真 mosdns 加载来保证
- 链路相关的一条负控存在覆盖缺口(该问题在本版之前就存在, 与本版改动无关)

## 2026-08-09 — v1.9.0（手机链路诊断）

以前手机连不上网关,只能一样样试:是 SIM 卡没走对?描述文件没装上?还是网关这头就没起来?
这一版在 Telegram Bot 里加了「📡 手机链路测试」——点一下,拿到一条一次性链接,在手机上打开,
回来就能看到网关**到底有没有收到**那次请求,以及请求是从哪个网段来的。

**Bot 新增手机链路测试入口。** Android 与 iPhone 都有,位置一样,给出的结论也用同一套说法。
服务器这头有问题的时候不会给你链接,而是先说清楚缺什么——省得你拿着链接在手机上白试半天。

**测试链接是一次性的,5 分钟过期。** 同一时间只允许一个测试在跑,开新的会让旧的立刻作废;
一条链接只能成功用一次。出结果、取消或过期之后,带链接的按钮会被撤掉,不会留在聊天记录里。

**网关能告诉你两件事**:这次的 HTTP 请求有没有到达;来源是不是落在你配置的内网卡段里。
就这两件,不多说。

**Android 补上了手机侧的探测组件。** 这东西以前只在 iPhone 上装(当初是为了区分双卡),
现在两个平台都有,所以两边的链路测试走的是同一条路、给的也是同一份判断。

**防火墙状态不再误报"配置漂移"。** 以前网关是拿防火墙打印出来的文字去比对的,系统只要把
规则改写成意思一样、写法不同的形式,就会被当成"有人动过",于是明明一切正常也报警。现在改成
按规则的实际含义来判断,这类假警报没有了。

**隐私**:测试链接里的那串随机码只出现在那一个按钮里——不写进状态文件、不写进日志、
也不出现在任何消息正文。来源地址只保留到网段一级,完整地址不落盘。

**升级不影响你已有的东西**:更新方式、配置、分流规则、证书与 Bot 凭据都照旧,
不需要重新设置任何东西。

### 这个测试能说明什么,不能说明什么

能说明的只有:网关收到了这次 HTTP 请求;它来自哪个网段。

**不能**据此认为:加密 DNS(DoT)是通的;SIM/APN 或移动网络正常;整体联网正常;
分流和出口正常。也不能断定发起请求的就一定是你本人的手机——它只能证明"有一台设备,
从那个网段,用这条一次性链接访问了网关"。

手机端的 DNS 查询证据(能证明"这台手机的 DoT 查询确实到了网关")**这一版没有**,
留待后续版本重新设计。本版本**不包含** Tailscale 相关功能。

## 2026-08-07 — v1.8.2(健康自检定时器可能再也不会运行)

`pdg-health.timer` 可能处在这样一种状态: `systemctl is-enabled` 说 enabled、`is-active`
说 active、`is-failed` 不 failed、`Result=success` —— 一切看着正常, 但它**再也不会触发**。
真机上曾这样静默停了 8 天, 期间服务异常也不会有 Telegram 通知。

只有两个字段露馅: `SubState=elapsed` 与 `NextElapseUSecMonotonic=infinity`。

- **定时器改用 `OnActiveSec=2min` + `OnUnitActiveSec=10min`**(去掉 `OnBootSec` 与
  `Persistent=true`)。`OnActiveSec` 相对**定时器自己被激活的时刻**, 这个参照点任何时候
  都在; 而 `OnBootSec` 在"开机很久之后才重启定时器"时那个点已经过去, 加上
  `OnUnitActiveSec` 不足以重新武装, 两条都算不出下一次 ⇒ 永久停摆。`Persistent=true`
  只对 `OnCalendar=` 生效, 这里留着是句空头承诺。检查频率不变, 仍是每 10 分钟。
- **更新迁移会重新排程, 并验证真的排上了**。换过 unit 之后不只是 `enable --now`
  (那对已经 active 的定时器什么都不做), 而是明确重启并确认存在有限的下一次触发时间;
  确认不通过就返回失败让更新回滚。内容没变且状态正常时不写盘、不重启。
- **`pdg doctor` 新增「健康自检定时器」检查**, 排不出下一次运行时报
  「健康检查定时器没有安排下一次运行。」正在执行的那一瞬不会误报。检查是只读的, 不会
  替你重启定时器 —— 自动重启只会把问题掩盖掉。

用户配置、分流规则、凭据与证书不受影响。

## 2026-08-04 — v1.8.1(v1.7.8 → v1.8.0 更新时, 首次启用救援平面会让整次更新回滚)

用户从 v1.7.8 更新, 停在这里:

```
→ 已切到发布 v1.8.0
刷新代码...
  首次启用救援平面(默认开; 之后可 pdg rescue disable)…
迁移(__migrate)失败, 回滚到更新前快照…
```

机器没坏, 是更新流程自己把自己锁死了。

- **更新子进程复用父进程那把全局锁, 不再重复去取**。`pdg update` 全程持着
  `/run/privdns-gateway.lock`, 中途要用**刚装好的新脚本**跑一次迁移
  (`bash /usr/local/bin/pdg __migrate`)。子进程继承了那个已经持锁的文件描述符, 却又重新
  `open` 了一次锁文件 —— 重新 open 得到的是另一个 open file description, 它并不持有那把锁,
  于是 `flock` 撞上父进程自己。首次启用或恢复救援平面正好要走这条路(`_rescue_enable` 会上锁),
  一撞就 `exit 1`;注意是 `exit` 不是 `return`, 所以迁移列表里的 `|| true` 一个字都拦不住,
  整个 `__migrate` 进程当场消失, 更新据此回滚到更新前快照。已经启用过救援平面、或者从没配过
  监听地址的机器不会走到那一步, 所以此前一直没暴露; `pdg update --dry-run` 也复现不了 ——
  它在取锁与迁移之前就返回了, 根本不跑迁移。
- **`sudo pdg __migrate` 单独执行时会显式取全局锁**。以前它一把锁都不取, 靠"反正下游某个
  函数会顺手锁上"兜着 —— 哪天某条迁移路径不再调用那个函数, 迁移就在裸奔, 而且不会有任何报错。
  现在两种来路各走各的: 由 `pdg update` 调起就复用继承来的锁; 用户手打就自己去取, 取不到
  (别的 pdg 操作正在跑)就报 BUSY 退出, 现网一个字节不动。
- **继承来的锁必须验明正身才认**: 文件描述符确实打开、它指向的**设备号与 inode** 就是那个锁文件
  本身、并且在这个描述符上**真跑一次非阻塞 `flock`** —— 三条缺一不可。只比路径字符串不算数
  (路径可以是符号链接、可以被 bind mount 换掉、文件也可能被删了重建), 只看"描述符号存在"更不算数。
  环境变量一律不作凭据。认错一次的代价就是两个进程同时改配置, 所以这里宁可退回去重新抢锁。
  没有继承关系的第三方(另一个命令行、Bot、配置事务)照样抢不到锁, 并发保护只紧不松。
- **用户配置、凭据与救援证书不受影响**: 本次修复不触碰任何配置内容, 出口、规则集、Bot token、
  自定义规则、平台标记逐字节不变; 救援平面的 token、证书与私钥**不会因此被轮换**, 证书指纹不变
  (指纹变了意味着用户下次访问要重新核对, 那是必须避免的)。

验证: 用 **v1.7.8 的真实更新器**升到本版(新 tag 只放在一次性发布源上, 逼更新真去拉取), 五种
救援平面状态各跑一遍 —— 有监听地址的首次启用、按来源段唯一地址自动认定、已启用但服务损坏后的
恢复、用户明确停用、没有可用监听地址的保守停用; Android 与 iOS 各一轮。救援平面另在**真 systemd
与真 nftables**下走完 socket 激活、崩溃后仍可进入、硬化生效、候选先校验后应用、来源双层防护、
启停与残留清理的完整验收。
## 2026-08-02 — v1.8.0(iPhone 上那份描述文件, 终于是"同一份"了)

日期取自 tag 与 Release 的实际时间(taggerdate 2026-08-02 14:55 +0800 / publishedAt
2026-08-02T06:55:15Z), 不是估的。

以前每次点"生成描述文件", 服务端都会造一份**全新身份**: PayloadIdentifier 与几个 UUID
都是随机的。iPhone 于是把它当成互不相干的新描述文件, 装一次多一份, 旧的还留着; 想更新
只能先手动删掉旧的。而服务端自己也说不清"手机上装的是第几版"——它根本没记过版本。

- **稳定身份**:一个 `instance_id`(uuid4)定终身, root/dns/ca 三个 payload 的 UUID 由
  `uuid5(NS, instance_id + ":" + 角色)` 派生。域名、IP、SSID、WLOC 开关都**不参与**推导 ——
  换了 DoT 域名也还是同一份描述文件的新版本, 而不是另起一份。
- **revision + digest**:只有**规范化后的语义输入**变了才递增版本。同样的配置重来一遍
  不会平白多一版, 界面也会明说"内容与上次完全相同"。
- **current / previous 两版历史**:留最近两版产物。`pdg ios previous` 能把上一版重新发给
  手机取件(早期它只把文件写在服务器上, 手机根本拿不到)。
- **发送前逐项校验**:内容、SHA256、mode、owner、身份、CA 指纹全对上才发。产物读取是
  **一次 open 读到底**(O_NOFOLLOW + fstat 查类型/nlink/mode/uid, 再从同一个 fd 读), 校验的
  字节与发出去的字节是同一份 —— 不给"检查完再被换掉"留窗口。
- **修复有边界**:`current` 只在能确定性重渲染且哈希一致时才允许修; `previous` 那一版用的
  根证书只在产物正文里有(元数据里只有指纹), 所以它**不用当前 CA 猜着重建** —— 宁可如实说
  修不了。
- **备份/快照/回滚把三件套当一组**:metadata + current + previous 不可拆分, 先联合校验候选
  再覆盖生产。少任何一件都会恢复出"记录说第 2 版、盘上躺着第 3 版"这种自相矛盾的状态。
- **Android 真机不依赖 iOS 模块**:那些模块在 Android 上**根本不安装**, bot 照样能导入运行,
  `pdg ios` 明确拒绝并说明本机是 Android。菜单里也没有 iOS 入口。

同版还修了一个分流优先级的老毛病:

- **自动生成的规则不再静默压过你点名的域名规则**。内核自上而下第一条命中即止, 而 WDA 解锁
  那批域名一直被插在最前面 —— 于是你在 bot 里把 `netflix.com` 指到 hkt, 配置里两条规则都在,
  实际却一直走直连, 面板和「测域名」都看不出来。现在自动规则排在点名规则之后, 老机器下一次
  改任何配置时会自愈, `pdg doctor` 也新增了「分流优先级」检查项。

**已知边界(没修, 也不假装修了)**:WLOC 的 W3 —— 服务端 MITM 改写成功, iPhone 最终仍显示
真实位置。它是独立议题, 不属于本版范围, 也**不能**被描述为"WLOC 端到端已通过"。

## 2026-07-30 — v1.7.8(全新机器装机时, 规则库写不进去导致整场安装回滚)

同一位用户的第三次报障, 这回卡在装机的"下载并解析 geosite 规则库"那一步:

```
REFUSED: 操作前这些硬门就是坏的: svc:mosdns, dns:127.0.0.1:53, port:853
mosdns: failed to load file /etc/mosdns/rules/geosite_cn.txt: no such file or directory
[x] 核心服务未能持续保持运行 → [!] 安装失败 → 已回滚到安装前状态
```

两个毛病叠在一起, 各修各的:

- **装机用修复模式开事务**:配置事务的 `normal` 模式有一道前置门 ——"操作前这些组件就是坏的,
  就别在坏东西上做普通变更"。这道门对日常更新是对的, 不能拆; 但**装机时** mosdns 本来就还没起、
  53/853 本来就还没人听, 那不是"坏了"而是"还没装完"。于是规则文件一个都没落盘。现在装机侧传
  `PDG_TX_MODE=repair`, `update-rules.sh` 据此选模式(默认仍是 `normal`, 日常更新一点没放宽);
  repair 允许降级基线, 但"操作前好、操作后坏"照旧整笔回滚。
- **geosite 文件先建成空的**:mosdns 的 `domain_set` 要求文件**存在**, 缺一个就 FATAL。所以下载
  一失败(没网、源站抽风、被墙)就不是"分类规则暂时为空", 而是"这台机器装不上"——重启 7 次后
  判定失败回滚。现在装机先把 4 个文件建成空的, 下载失败只告警并说清影响与补救办法, 安装继续。
- **崩溃重启循环的机器终于修得动了**:事务在拍"操作前状态"快照时会拒绝处于过渡态的服务 ——
  快照拍在半空中说不清回滚目标。但 systemd 把两种截然不同的处境都记成 `ActiveState=activating`:
  `SubState=start`(真在启动中, 该等)和 `SubState=auto-restart`(**已经死了, 在等下次重试**)。
  mosdns 缺一个规则文件就 FATAL, 于是永远停在后者 —— 要修就得写规则文件, 写规则文件要开事务,
  事务又因为"它在 activating"而拒绝, **两种模式都拒**, 谁也救不回来。现在按 `SubState` 区分:
  崩溃重启循环视为稳定的"没在跑"(回滚目标同样明确), 真在启动中的照旧等。
- **bot 的「更新规则库」被硬门拒时按修复模式重试一次**并如实告知(repair 只放宽**基线**,
  "操作前好、操作后坏"照旧整笔回滚)。定时任务不改, 仍是 `normal` —— 无人值守时不该在坏掉的
  机器上硬来。
- **`pdg doctor` 新增「geosite 规则库」**:装机那行黄字滚过去就没了, 而"全都走代理 / 国内网站变慢"
  这种症状跟"规则库是空的"对不上号, 用户很难自己想到。缺文件判 **fail**(下次重启起不来), 全空或
  部分为空判 warn, 都直接给出"在 bot『更新规则库』跑一次"。

为什么两台线上机测不出来: 它们都是老装 + `pdg update`, 从没走过全新安装这条路; e2e 沙箱又预先建好了
geosite 文件、且装完之后硬门是好的。新增的 e2e 场景 8/8b 专门造出装机现场的基线(mosdns 未运行)并直接
跑真的 `update-rules.sh`, 两个方向都验: `normal` 必须拒(门没被拆)、`repair` 必须写得进去。

上面"崩溃重启循环"那条是在**真 Debian 13 容器**里发现的 —— 沙箱的 systemctl 桩一 restart 就好, 表达不出
`activating(auto-restart)` 这个状态, 所以先前那版"死锁已解开"其实并不成立。测试桩现在支持 `SubState`,
崩溃循环与真启动中两个方向都有断言。

## 2026-07-30 — v1.7.7(网关上还跑着别的服务时, 终于有地方放自定义放行规则)

用户报障续: 他那台机器内核里 `inet filter` 的 input 链有 `tcp dport 80 accept`, 装机因此中止 ——
这次**拦得对**(本项目的 `table inet pdg` 是 `policy drop`, 不放行公网 80, 装上去他那条会被架空)。
错的是给的建议: "把需要的放行并入 `table inet pdg` 的 input chain" —— 那张表每次装机/迁移都按模板
重建, 手加进去的规则下次就没了, **建议本身行不通**。

- **装机自动搬运你已有的放行规则**:这不是 80 端口的问题 —— 实测 80 / 443 / WireGuard 51820 /
  node_exporter 9100 / 甚至 SSH 22, 只要外来 input 链里有一条我们不放行的规则就会拦。关键认识是:
  问题不在"你有自己的 input 链", 而在本项目的 `policy drop` 架空了你的 `accept`。所以装机现在把那些
  `accept` **复制**一份进下面那个目录 —— 你自己那张表一个字节都不用改(原规则留着只是冗余)。
  搬不动的照旧中止并逐条点名: `drop`/`reject`(搬过去等于给你加限制)、`limit`/`log`/`jump`(复制一份会把
  原来的语义绕过去)、以及引用了你表内 set/链的规则(先在一张试验表里 `nft -c` 验过才落盘)。
  `PDG_NO_ADOPT_RULES=1` 关掉这个行为。
- **新增 `/etc/privdns-gateway/nft-input.d/`**:放这里的 `*.conf` 会被 include 进 `pdg` 的 input
  chain 末尾(`policy drop` 之前), 且不受 `pdg update` 影响。装机建目录并附说明; 老机器 `pdg update`
  时补上 include 点(幂等、`nft -c` 先校验、失败还原; pdg 表里没有 input chain 这类认不出的形态一律
  不动)。用 glob 而不是单文件 include —— 实测单文件缺失会让整份 `nftables.conf` 加载失败, 那等于把
  人锁在门外; 空目录则照常加载。
- **`pdg doctor` 新增「自定义放行」**:目录里有 `.conf` 但配置里没有 include 行时判 **fail** —— 那是
  最坏的一种, 用户以为规则生效了, 实际一条都没进内核, 而且哪儿都不报错。
- **装机冲突报错给出可执行的两条路**:挪进上面那个目录, 或者确认不需要就删掉; 不再给那条行不通的
  建议。
- **`nftscan` 报错贴出具体规则**:以前只说"1 条规则", 用户不知道是哪条、也就无从判断怎么办。
- **本项目已放行的那几条不再算冲突**:外来 input 链如果只有 `iif lo accept` /
  `ct state established,related accept` / `icmp accept` 这类我们自己也放行的规则, 装上去不会架空
  任何东西。发行版自带的骨架基本就是这几条。带端口的放行不在此列 —— 扫描发生在问 SSH 端口之前,
  这里无从判断那个端口是不是我们也会放行的那个, 判错的代价不对称。

## 2026-07-30 — v1.7.6(全新 Debian 13 / Docker 主机装得上了)

用户报: 全新 Debian 13 首次装机中止在"`flush ruleset` 会冲掉只存在于运行中的表 —— `table ip nat`、
`table ip filter`"。

- **空壳表不再拦装机**:那两张 `ip` 表是 iptables-nft 建的 —— Debian 上 `iptables` 默认走 nft 后端,
  cloud-init、包的 postinst、甚至一句 `iptables -L` 都会把它们建出来, 链在、策略 accept、一条规则
  都没有。冲掉什么也不丢, iptables-nft 下次用到自己重建。拦它等于**全新机器装不上**。判据改成与
  `nftscan.py` 同源(那边早就按"空骨架不算冲突"处理了): 有规则、或策略不是 accept 才算真会丢。
- **Docker 主机自动跑通**:表里真有规则(Docker / fail2ban / libvirt / k8s)时, 不再要求用户先自己
  动手改防火墙文件 —— 装机会把 `flush ruleset` **注释掉**(保留原文, 并在文件里写清是谁改的、
  为什么、怎么还原), 必要时给你自己带规则的表补上 `table X` + `delete table X`, 让每张表各自重建,
  去掉全局 flush 之后重复应用也不会累积规则。本项目自己的块从来就是这个写法, 不依赖那行 flush。
- **仍会中止的一种**:那张表**和别人共管** —— 内核里有文件没声明的链(你写了
  `table ip filter { chain FORWARD }`, 而 Docker 又往 `ip filter` 里加了 `DOCKER-USER`)。给它加
  `delete table` 会把别人那部分一起删掉, 没有安全的自动解法, 报错会点名是哪张表。解析不确定同样
  fail-closed。
- **`PDG_KEEP_FLUSH=1`**:不希望我们动 `/etc/nftables.conf` 时设它, 遇到冲突会中止并给出手工做法。
- 报错文案按归属分流: 认出 Docker 这类**动态**规则时, 只给"去掉 flush ruleset"并附可粘贴的命令 ——
  把它们抄进 `/etc/nftables.conf` 是错的做法(随容器起停不断变, 下次就对不上), 老文案两条并列给,
  用户很容易选错那条。

## 2026-07-30 — v1.7.4(规则集在 gfw 模式下不再是死规则)

- **规则集的域名自动进 mosdns 劫持表**:此前规则集只写 mihomo 那一侧, 而流量能不能到 mihomo 由
  mosdns 决定。`all` 模式下"不是国内就劫持"顺带把它们兜住了, 看不出问题; `gfw` 模式下劫持集只有
  被墙域名 —— 规则集里的域名拿到真实 IP、手机直连, 那条 `RULE-SET` 规则永远匹配不到。规则加了、
  UI 说成功了、`pdg doctor` 也绿, 就是不生效。现在加/删/刷新规则集与恢复备份时, 都会在**同一笔
  事务**里按启用中的规则集重算 `ruleset_hijack.txt`; 老机器 `pdg update` 时补算一次。
- **`.mrs` 也能派生**:它是 mihomo 的二进制格式(succinct trie + zstd), 自己解析等于把内核的数据
  结构抄一遍; 但内核自带的 `convert-ruleset <behavior> mrs <in> <out>` 正好是反方向 —— 输入
  `.mrs`, 输出文本域名清单(实测 8.9KB / 1042 条约 12ms)。用它就不必另写解码器, 也不会与内核
  版本漂移。`+.x.com` → `domain:x.com`、`x.com` → `full:x.com`、`*.x.com` 放宽成 `domain:x.com`
  (多劫持一点只是让流量进 mihomo, 出口仍由 mihomo 的规则决定)。`ipcidr` 类型本来就没有域名,
  跳过而不是报错。只有真读不出来的(文件损坏、类型认不出、缺 mihomo 二进制)才由 Bot 与
  `pdg doctor` 点名。
- **手写过的 `ruleset_hijack.txt` 不会被覆盖**:表头不是自动生成的那行就当作管理员自己维护的数据,
  迁移只提示、不动手。
- `pdg doctor` 新增「规则集劫持表」一项:与启用中的规则集不同步时, `gfw` 模式判 fail、`all` 模式判 warn。

## 2026-07-30 — v1.7.1(点名的分流规则优先于国内判定)

在 Bot 里把域名指到出口、内核里那条规则也确实写进去了,可流量就是不走它 —— 这一版修的是这个。

- **明确指定的分流规则不再被 `geosite_cn` 抢先判直连**:mosdns 里新增「明确代理集」
  (`custom_hijack.txt` + `ruleset_hijack.txt`),排在国内判定**之前**。此前用户点名的出口域名只在
  `hijack_set` 那道门被查,而那道门在 CN 判定之后 —— 上游 geosite 一旦把某个二级域整体归进国内
  (现场实例:整个 `byte-test.com`),DNS 就先返回真实地址,流量根本不进 mihomo,内核里那条 route
  规则永远匹配不到。表现是"规则在、`pdg doctor` 全绿、就是不生效",没有任何一处会报错。
  固定优先级现在是:WLOC/MITM 强制接管 → 点名的代理域名 → `geosite_cn` → all/gfw 自动海外判断。
- **命中后的 DNS 行为**:A 返回网关地址,AAAA 与 HTTPS/SVCB 返回空。走**独立**的劫持序列,不是复用
  MITM 那条 —— 普通代理域名只是进 mihomo,出口由数据模型里的真实规则决定,不会被送去 `pdg-mitm`
  终止 TLS。改判直连时仍会同时清掉旧的劫持记录(否则新顺序下"设为直连"会静默失效)。
- **老机器迁移**:`pdg update` 会幂等补齐插件与执行顺序,保留 DNS 上游、既有规则、规则集与自定义
  文件。走 pdgtx 事务:候选先由真 mosdns 校验,失败或重启不健康则整笔回滚,现网配置一个字节不动。
  **高度自定义、无法安全识别的 mosdns 配置一律不猜着改**,由 `sudo pdg doctor` 的「明确代理优先级」
  一项点名,并给出手动补法。
- **`ruleset_hijack.txt`**:新增的规则文件,装机/迁移会建空文件,重装与备份都保留它。**目前项目不会
  自动往里写** —— 规则集下发的是 mihomo 的 `.mrs`,域名清单不在网关侧展开。它是给管理员手动补的
  槽位,优先级与 `custom_hijack.txt` 相同。
- **测试**:真起 mosdns 验证「同时命中 `geosite_cn` 与明确代理集的域名 A 进网关 / AAAA 与 HTTPS 为空、
  同 zone 未点名的兄弟域名仍走直连、普通国内域名不退化、WLOC/MITM 仍最高、all 与 gfw 两种模式」;
  另有一条负控:把那道判断从渲染产物里删掉后,上述断言必须**恰好**变红而对照断言仍绿。迁移侧覆盖
  成功、重复迁移幂等、候选校验失败不改现网、服务重启失败整笔回滚,以及自定义形态下 doctor 点名。

- **陈旧的未完成事务不再挡住配置迁移**:定时 geosite 更新会留下"开了、暂存了候选、从没应用过"的
  PREPARING 事务(两台线上机器上各有几笔)。pdgtx 自己不把这种状态算作需要收尾, 但 `pending` 会把它们
  打印出来"仅作提示" —— 迁移原先拿输出是否为空当判据, 于是会在**恰恰最需要修的机器上**静默跳过:
  `pdg update` 照样报成功, doctor 照样告警, 分流照样不生效。改用 pdgtx 的退出码判定, 真正需要人工
  收尾的事务仍会挡住迁移, 并把挡路的事务 id 打出来。
- **修好 Mihomo 后端下 WDA 开不了**:WDA(流媒体解锁走中继)此前给 model 加的是一个本地
  `rule_set=unlock`。那个临时规则集没有 provider 元数据, mihomo 渲染器会正确地拒绝它 —— 于是
  Telegram Bot 里点「🔓 解锁走 WDA」永远失败。改成直接内联域名规则, 渲染出的就是真实的
  `DOMAIN-SUFFIX,…,jp`。老机器上的 `rule_set=unlock` 状态可幂等迁移, WDA 域名清单跨版本变化时
  按现网 `unlock.txt` 精确移除上一版规则; 遗留的 `unlock.json` 在同一笔事务里删掉。
- **WDA 规则不会被单域名增删误伤**:内联之后 WDA 在 model 里就是一条普通的 `outbound=jp` 规则,
  而"把域名指到出口"找的正是第一条同出口、无规则集的规则 —— 用户加一条 `x → jp`, x 就会被并进
  WDA 那条; 删规则键盘也会把 55 个 WDA 域名列成可删项。任何一次这样的改动都会让 WDA 状态显示成
  "落地出口"(其实还开着), 点关闭只清空 `unlock.txt` 而域名继续走 jp。现在增删单域名一律绕开 WDA
  规则, 开关保持幂等。**数据模型形态没有变化**, v1.7.0 用户可直接更新, 回滚也不受影响。

**如实说明一处边界:** 本项目**不会**自动识别字节跳动(或任何厂商)的国内/海外业务。上游 geosite
把整个 `byte-test.com` 归为国内,这一版让**管理员明确添加的**定向规则能够生效,并没有让网关学会
分辨这类业务 —— 需要走代理的子域仍要逐个添加。

## 2026-07-29 — v1.7.0(独立救援平面与可靠性收口)

网关自己出问题时,管理入口往往跟着一起没。这一版加了个不依赖 mihomo / mosdns / Bot / tailscaled 的独立 HTTPS 页面:socket activation 拉起、只监听内网地址、可以看状态与配置事务、必要时按快照恢复受管配置或做紧急完整恢复。默认关闭,`sudo pdg rescue enable` 开。

- **访问控制两层**:nft 只放行内网卡来源段到 bind 地址的 8446,服务本身在 TLS 握手之前再按内核给出的对端地址判一次(不读 `X-Forwarded-For`)。放行规则带项目标记内联在 `table inet pdg` 里 —— 单独建表不行,同一 hook 上多条 base chain 都会执行,别人的 `policy drop` 会把独立表里的 accept 架空。
- **指纹核对**:自签证书只保证连接被加密,不证明对面是谁。README、登录页、CLI 与 `docs/rescue-plane-access.md` 用同一套说法:指纹必须从 SSH 上 `pdg rescue fingerprint` 独立取,页面上显示的那串不能拿来核对页面自己;浏览器看不到完整 SHA-256 就不要输 token。`rotate cert` 后指纹必然变化并需重新核对,`rotate token` 不动指纹但立即作废所有会话。
- **恢复仍然是事务**:受管配置恢复走完整 pdgtx 事务(候选校验 → before-image → 落盘 → 服务动作 → 观察 → COMMITTED 或整体回滚);有未完成事务时拒绝新的写入并指向 `pdg tx recover`;进程被 SIGKILL 也能靠 before-image 收尾,内容、权限位与属主一并还原。
- **快照结构只识别、不转换**:`legacy-dnsdist` 拒绝受管恢复,完整恢复要手输快照末 6 位;`unknown` 一律拒绝。同时带上两套结构特征的快照判为 `ambiguous:…` 并**两条路径都拒**——原先写成顺序 if,先匹配到哪条算哪条,这类包会被当成当前格式放行。
- **卸载不再吞掉用户的防火墙改动**:卸载只摘掉项目自己的管理块与带标记的规则,`/etc/nftables.conf` 里用户后加的规则逐字节保留,不再整份套回 `.pdg-orig`;卸载前的配置备份留在 `/var/backups/` 下,0600。

- **统一配置事务与崩溃恢复**:出口、分流、规则集、DNS 上游、防火墙、TFO、证书、WLOC 开关、备份恢复等所有会改动生产配置的操作都在一笔 pdgtx 事务里完成 —— 候选先校验、原子落盘、按目标状态拉起/停掉服务并观察健康门,任一步失败整体回滚;进程被杀也能用 `sudo pdg tx recover <id>` 收尾,`sudo pdg doctor` 会点名未完成的事务。
- **紧急默认出口**:代理链整体不通时,可在救援页把默认出口切回本机直出,同样走事务。
- **静态文件单一真源**:`/opt/pdg-bot` 下的项目静态文件由 `lib/modules.sh` 一份清单驱动,安装、`pdg update`、卸载三方共用(Android 22 项 / iOS 27 项;卸载另收走一个早期版本遗留的 `nftpurge.py`)。以前 Bot 本体、MITM 组件、探测脚本等十来个文件只在装机时各写一行装上去,升级从不同步、卸载也不删。清单会在部署前整份校验:重复目标名、非法 mode、目标逃出受管目录、源路径逃出仓库一律拒绝,半装比不装更糟。
- **`PDG_FORCE_REINSTALL` 保护用户数据**:覆盖重装不再清掉 `bot.env`、数据模型与规则集。
- **卸载保留用户的防火墙改动**:只摘项目自己那段管理块与带标记的规则,`/etc/nftables.conf` 里用户后加的规则逐字节保留,不再整份套回 `.pdg-orig`;卸载前的配置备份留在 `/var/backups/` 下(0600)。
- **Mihomo / mosdns / 平台隔离**:sing-box 数据模型到 Mihomo 的渲染、mixed 规则条件语义、`rule_set` 数组与旧条目、出口增删改名与故障组、mosdns 限速与上游、DoT 证书部署都补了回归;iOS 专属组件不会装到 Android 机器上,反之亦然。WLOC 切地点只原子替换 `mitm.json`、不重启服务,开关 WLOC 才走完整事务。
- **测试与 CI**:E2E 的破坏性操作必须落在本轮创建、带 nonce 标记的一次性根内(`PDG_E2E_ISOLATED=1` 不再被当作隔离证明);更新失败矩阵覆盖 checksum、截断、解包安全边界、manifest 结构错误、`py_compile` 失败与逐个静态文件部署失败,并由一份统一的系统不变量捕获器按 profile 比对前后状态 —— 未声明的变化即失败。

**如实说明两处边界:**

- iOS / Android 浏览器上的证书指纹核对界面**尚未做过真设备全流程验证**。服务端与 TLS 侧的行为(指纹值、`rotate cert` 后指纹改变、按旧指纹固定连接会被拒)已实测;设备端的按钮位置与能否看到完整 SHA-256 因系统版本而异,文档按版本中立表述给出替代方案。
- 本版发布前的更新验证全部在本地模拟发布源上完成。**通过真实 GitHub release 走一次 `pdg update`,要在 v1.7.0 发布之后验证。**


## 2026-07-26 — v1.6.2(安全边界与失败可见性:出事要么不动手,要么说清楚)

本版没有新功能,集中修一类问题:**出错时看起来成功**。更新、回滚、恢复、装机、平台切换都做了同样的收口 —— 要么在改动现网之前中止,要么完整回滚并说出真实原因。

- **更新 / 回滚 / 恢复 / 解包**:`pdg_write_unit` 改为原子写(跨版本回滚不再把 unit 清成 0 字节);`pdg update` 留精确快照并按目标回滚,git 复位失败、必需文件装不上、迁移失败、内核起不来各自带原因并还原;恢复备份是完整事务,任一步失败逐字节还原,还原不完整不再谎称"全部还原";备份解包改流式,路径逃逸、符号链接、硬链接、设备文件与压缩炸弹一律拒整包,限额可调但关不掉。
- **第三方 sing-box 保护与旧装收口**:归属判据集中到 `lib/singbox.sh`,只删能证明是本项目装的;运行时归属与数据模型归属分开判(v1.6 起不装 sing-box 运行时,但 `/etc/sing-box` 里仍是本项目的出口数据);判不出归属时列出保留了什么、为什么、怎么手工清理。sing-box→mihomo 迁移遇到无法无损转换的出站会点名并回滚,不留半迁移态。
- **nftables input 链冲突**:判据抽成单一实现(`deploy/bot/nftscan.py`),安装前置门、迁移与 doctor 共用。同一 hook 上的多条 base chain 都会执行,别人的 input 链会架空本项目的放行 —— 检出即中止,且中止发生在创建任何配置、二进制、unit 之前。读不到运行 ruleset 时如实报"无法确认"而不是当成干净;Debian 自带的空骨架不误判为冲突。合并防火墙配置只替换 `table inet pdg`,用户的其它表逐字节保留。nft 的位置改为与扫描器同一份判据,`/usr/sbin` 不在 PATH 时不再当成"没装 nft"而跳过校验。
- **规则集**:`.mrs` 的 behavior 从二进制头识别,老条目自动补齐;支持真实 YAML provider;进不了运行配置的规则集必须失败,刷新失败如实上报,不再静默丢弃。
- **WLOC(iOS)**:切换地点走热路径 —— 只原子更新配置,`pdg-mitm` 不重启、内核不重渲、一次 DNS 都不断;插件改为每次请求整份读配置(不再靠 mtime 判变化,连续两次修改不会漏读);切换后按 generation 确认手机是否真的来过请求,并如实区分"还没来"与"来了但没改到";后台监听只编辑自己那条消息,原消息被删就安静结束,不再覆盖菜单或补发新消息。
- **CLI / doctor / 平台切换**:Bot 凭据三态统一到一份判据 —— 两项都空是合法的禁用态、只配一项才是配置错误,没配 bot 的机器不再因此升不了级或切不了平台;`pdg restart` 先校验内核配置再重启,起不来返回非 0 并点名;`pdg platform` 全程事务化,组件装失败即整体回滚;`pdg detect-cidr` 事务化;`pdg update --dry-run` 只读且严格,拉不到 tag 就返回非 0 而不是报"已是最新";仓库读不到时版本显示为"未知"而非空白。
- **测试与 CI**:新增装机 nft 冲突、平台切换、CLI 操作三组端到端用例;E2E 在 CI 拆成一脚本一容器的矩阵,另加一条串行守卫盯脚本之间互相踩;跨版本回滚对受影响的旧版逐个跑而不抽样。同时堵掉测试自身的假绿 —— 函数抽不到、命令不存在、退出码非 0、零断言却退出 0,现在都判失败;CI 的容器 job 改为在 checkout 之前装好 git(否则 checkout 退化成下载 tarball、工作区没有 `.git`,跑装机的 E2E 会因此中止),`.mrs` 的"没有 zstd"负向用例也自己造齐前提(连 python 的 zstd 实现一并挡掉),不再依赖跑测试的机器碰巧没装。

## 2026-07-25 — v1.6.1(装机识别内网卡段: 抓包与手输并行,谁先出结果用谁)

真实装机踩出来的三件事,都在「识别内网卡来源段」这一步:

- **抓包与手输现在并行,谁先给出结果就继续下一步**。抓包最长 90 秒,而多数人**早就知道**自己的网段 —— 旧实现只能干等满 90 秒、抓不到再手填,白等一次。现在开抓的同时就接受输入:手输先到就掐掉抓包直接用,抓包先到就用抓到的(不再多问一遍确认),两条路都不必等对方。
- **"可以直接手填"的提示挪到抓包期间**。旧实现等抓完失败才说"可先手填",而那时 90 秒已经花掉了。现在提示就印在抓包说明里(`★ 已经知道网段? 现在就能直接输入(如 172.22.0.0/16)并回车 —— 不必等抓包结束`)。
- **填错/没填不再直接判死整场安装**。旧实现在等满 90 秒后,一个空回车就 `die` + 回滚全部改动,得从头再来。现在最多重问 3 次;无可用终端时直接给出 `PDG_INTERNAL_CIDR=...` 的出路,不空转。
- **新增 CIDR 形态校验**:`172.22.0.0`(漏了 `/16`)、`172.22.0.0/33`、`256.1.1.1/16` 这类会渲染出"谁都匹配不上"的防火墙/mosdns 配置的输入,在入口就拦下并说明正确形态 —— 以前会一路装完,直到发现分流全不生效才回头查。

**实现**:新增 `lib/cidr.sh` 作为取值单一事实源(校验 + 抓包/手输竞速),`install.sh` 与 `pdg detect-cidr` 共用,两处不再各写一套;竞速时若抓包先到,会清掉用户敲了一半的残留,免得漏进下一个提问(平台/token)当答案。无可用终端时退化成纯抓包,非交互装机行为不变。

**测试**:新增 `tests/test-cidr-input.py`(7 项,在**真 pty** 里验 —— `read -t` 的竞速行为与"半截输入不外漏"用管道复现不出来);另在沙盒里真跑了一遍交互安装:抓包期间 2.2 秒手输即被采纳、总耗时 9 秒装完(旧实现要等满 90 秒),填错格式则重问后正常装完。

## 2026-07-25 — v1.6.0(彻底移除 sing-box 运行时,mihomo 成唯一内核)

**破坏性变更**。sing-box 被钉死在 1.12.x 是个死胡同:1.13 移除了本网关赖以工作的 `sniff_override_destination`,而它的新写法(`action: sniff`)**不覆盖目标地址**、会导致流量回环 —— 也就是说 sing-box 这条路只会越走越窄。mihomo(clash.meta)的 `sniffer.override-destination` 没有版本天花板、活跃维护,且 iOS 位置改写(WLOC)本就只有 mihomo 有路由层。故本版把 mihomo 从"可选内核(原型)"提为**唯一内核**。

- **旧的 sing-box 机器 `sudo pdg update` 时自动迁移**,无需任何手工操作:下 mihomo → 从现有配置渲染 → `mihomo -t` 校验 → 换 nft 入站模型(REDIRECT→7893)→ 起 mihomo 停 sing-box → 删掉 sing-box 的 unit 与二进制。**出口、分流规则、故障组、证书、DoT、mosdns 全程不动**(数据模型共用,见下)。
- **迁移是事务性的,且绝不静默丢出口**。遇到 mihomo 无法无损转换的出站(如 wireguard/shadowtls),迁移**逐个点名**该出口并返回失败 → `pdg update` 回滚到更新前快照,用户仍留在旧 sing-box 版本、数据无损;在 bot 里删/换掉该出口后重跑 update 即可。渲染异常、`mihomo -t` 不过、nft 应用失败、内核起不来同样各自带出**真实原因**并回滚,不留半迁移态。
- **`pdg switch-core` 命令与 Bot 的换核菜单项一并移除**(只剩一个内核,切换已无意义)。`lib/units.sh` 的 `pdg_unit_singbox`、`lib/versions.sh` 的 `SINGBOX_VER`/哈希、`deploy/firewall/nftables.conf`(sing-box 变体)一并删除。
- **`PDG_CORE` 环境变量取消**,装机恒装 mihomo;README 一键安装命令**形态不变**。
- **`/etc/sing-box/config.json` 仍是 bot 的唯一数据模型**(mihomo 配置由 `sb2mihomo` 从它渲染),`/etc/sing-box/{config.json,rs,ui}` 路径保持不变 —— 因此备份/恢复、快照回滚、面板全部照旧可用,跨版本快照也仍能回滚(回滚一律按 mihomo 起核,并清掉快照可能带回的 sing-box 残留)。
- **自动迁移的健壮性**:现网 nft 认不出 SSH 端口时(自定义/异形防火墙)不再直接判死 —— 那会把用户永久挡在旧版上;改为退回问 sshd 实际在听哪个口,再退回 22。

**测试**:`tests/test-switch-core*.sh` / `e2e-switch-core.sh` 重定位为迁移测试 —— 新增 `test-migrate-drop-singbox.sh`(14 项:各类失败都要点名原因+返回非0+回滚标记+不误删 sing-box;成功要清干净;幂等)与 `e2e-drop-singbox.sh`(**真** mihomo 二进制 + 3 个真实协议出口:迁移后一个不丢、`mihomo -t` 通过、防火墙已换模型;注入 wireguard 出口必须点名拒绝且不留半迁移态);`e2e-update.sh` 现在整条 `pdg update` 都跑在"老 sing-box 机器"上,断言更新后已迁为 mihomo;`test-outbound-schema.sh` 改为验证**真实生产链路**(parse_link → sb2mihomo → 真 `mihomo -t`),11 个协议全过。

## 2026-07-24 — v1.5.13(交互全新装挂在平台探测的 `cat`,issue #2)

**用户报告修复(issue #2)**:新装机交互式安装,在识别到内网卡网段后立刻失败并回滚,屏幕上只有"安装失败 → 回滚",没有任何真原因;而**非交互式安装一切正常**。日志停在这一句:

```
[*] 抓到内网卡段: 172.22.0.0/16
内网卡来源段 CIDR [172.22.0.0/16]: [!] 安装失败 → 回滚本次全新安装的改动…
```

- **根因(真凶,端到端沙盒复现)**:CIDR 的 `read` 其实**成功了**(回车→空→回落到刚抓到的网段);紧接着的平台探测行 `_ep="$(cat /etc/privdns-gateway/platform 2>/dev/null)"` 才是致命点 —— 全新装时该文件尚不存在,`cat` 返 1,而 `set -euo pipefail` 下"**赋值里命令替换失败**"是致命错误,EXIT trap 当场接管转入回滚,真原因被埋掉。平台 `read` 的提示还没来得及打印,所以日志停在 CIDR 提示后 —— 报告里"疑似 CID 的 read 没取到输入"是被这个界面顺序误导了。**非交互式安装因为预置了 `PDG_PLATFORM` 而跳过这行 `cat`,恰好绕开**,这也解释了它为何一切正常。该行由平台隔离特性(commit `3f75dfe`)引入,是交互全新装路径上的近期回归。
- **修复**:`cat` 加 `|| true`,让这句可选文件读取在 `set -e` 下永不致命。
- **一并加固交互输入(防御纵深)**:全部交互 `read` 换成容错的 `ask <变量> <提示> [默认]` 助手 —— 每次**新开干净的 `/dev/tty`**,把 **EOF / 无可用终端一律当"用默认值"**处理,绝不触发 `errexit`。这样即便真遇到管道 stdin / 无终端等场景(此前任一交互 `read` 撞 EOF 都会炸掉整场安装),也会优雅回落到探测值/默认值(等同非交互),而不是无源可查的"安装失败";签证书前「回车继续确认 A 记录」处同款隐患一并收口(撞 EOF/无终端直接继续,`Ctrl-C` 仍能中止)。
- **测试**:`tests/e2e-install.sh` 新增交互全新装用例 —— 真跑 `install.sh` 交互模式(不预置 `PDG_*`、`stdin` 无输入),断言撞到 EOF 也能装完(回落探测值/默认值)、不回滚。回归验证:改前该用例复现回滚失败,改后转绿。

## 2026-07-22 — v1.5.12(端到端沙盒 + 它查出来的四个真问题)

新增一层端到端测试:用 user+mount namespace + overlayfs(CI 上用一次性容器)把 `/etc` `/opt` `/usr/local/bin` `/var/lib` 覆盖掉,于是能在**真实绝对路径**上跑真正的 `install.sh` / `pdg update` / `pdg __migrate` / `switch-core` / WLOC 事务,而宿主毫发无损。之所以要这层:此前的回归都在函数级别打桩,**跨组件的接缝没人看着** —— 而实际报障(分流规则不进 mosdns、backend 标记从不落地、GMS 重复插入)全是接缝问题,单测全绿照样漏。六条用例共 88 项断言,已接入 CI 且**确认在 CI 里真跑**(不是跳过)。

沙盒查出并修复:

- **没配 bot token 的机器永远升不了级**(v1.5.10 引入的回归)。`doctor.py` 的约定是「有 fail → 1,否则 0」,更新的校验门却把退出码 1 当成"命令跑挂了" → 直接绕过后面按 JSON 的判定,包括「未配 token 时 pdg-bot 未运行」那条豁免;失败清单也拿不到,只剩一句"自检命令执行失败"。现在只把 0/1 之外的退出码(崩溃/找不到/被杀)当异常。
- **`/etc/resolv.conf` 删不掉就装不上**。LXC / 部分 Docker 与云镜像把它 **bind-mount** 进来,`rm -f` 返 EBUSY —— 在 `set -e` 下是致命错误,安装当场中止转入回滚,屏幕上只有"安装失败 → 回滚",真原因被埋掉。改为删不掉就**原地覆盖内容**;连写都写不进去(只读挂载)也只告警不中止 —— 那只影响网关自己解析走哪个上游,转发链路不受影响。回滚那侧同样退化成把备份内容写回。
- **`sb2mihomo` 静默丢弃不认识的出站**。换核时"出口凭空消失"而无人报错;现在不认识的类型点名列出并拒绝。
- **老装升级**:恢复备份按本机平台净化;**推测**出来的平台不做破坏性清理(猜错就删了对方的组件);GMS 幂等判据不再依赖 JSON 文本格式。

## 2026-07-22 — v1.5.11(`all` 劫持模式恢复原语义 + 模式切换不再空转报成功)

接着 v1.5.10 修 issue #1。v1.5.10 补的用户劫持表让**显式指到出口**的域名能生效,但没解决"为什么它们一开始就不生效" —— 根子在 `all` 模式本身。

- **`all` 恢复排除式劫持**。网关本来的形态是:`force_hijack` → `geosite_cn` 直连 → **其余全部劫持进代理**(线上老机器至今仍是这个形态,行为一直是对的)。加 gfw 劫持模式时,`all` 被一并改成了**白名单式**(`hijack_set` = `geolocation-!cn`),模板注释还写着"非CN都在集内,此门基本不放行"——这个假设是错的:`geolocation-!cn` 是 geosite 的**策展分类**,任意/个人域名根本不在里面。于是 `all` 静默退化成一个比 gfw 还窄的模式,用户加的分流规则看起来"没生效"。现在 `all` 不再装 `!qname $hijack_set` 那道门。
- **`pdg hijack-mode` 不再空转报成功**。它原本靠 `sed` 替换配置里的 geosite 文件路径实现;老形态机器没有 `hijack_set`、也没有那两个文件的引用,`sed` 一个字都改不到,却照样打印「✅ 劫持模式 → xxx」并把模式写进 `profile.env`。现在形态没变就直说「配置已是 X 形态,无需改动」,认不出的自定义形态明确拒改并返回非 0。
- **老机器补齐 `hijack_set` 插件**。老形态没有它,`gfw` 模式无从实现;迁移会补上(劫持行为不变),从此两种模式都能切。
- **实现方式**:新增 `lib/mosdns.sh` 作为劫持形态的单一事实源(仿 `lib/units.sh`),`install.sh` / `pdg hijack-mode` / 老装迁移三处共用,双向幂等,认不出的形态一律不动。
- **测试**:新增真起 mosdns 的形态回归 —— `all` 下"不在任何 geosite 分类里的域名"必须被劫持到网关,`gfw` 下集内劫持、集外放行;退回白名单式 `all` 该项立刻转红。

## 2026-07-22 — v1.5.10(分流规则真正生效 + 安装/更新/换核失败窗口收口 + 失败诊断可读)

**用户报告修复(issue #1)**

- **分流规则加了不生效**:在 bot 里把域名指到出口后仍走直连(现场:`ip.skk.moe → jp` 已回"已把…",再查仍是「国内直连(mosdns 返回真实 IP)」)。根因是 `add_rule` 指到**出口**时只改内核路由、完全不碰 mosdns;而 mosdns 的 `hijack_set` 只装 geosite 的**策展分类**,不在集内的域名返真实 IP → 手机直连、流量根本不到网关,内核里那条规则永远命不中(指到 `direct` 反而会写 `custom_direct.txt`,正是这个不对称让问题一直没暴露)。新增用户劫持表 `custom_hijack.txt` 并入 `hijack_set`:指到出口即写入、改判直连即移出、删规则即清除,写完重启 mosdns(`domain_set` 只在启动时加载)。**老装迁移会回填已有的显式出口域名**,此前加过却一直没生效的规则更新后立即生效,无需重加。
- **`hijack-mode all` 看起来失效**:同一根因。`all` 从来不是"所有非 CN 域名",而是"geosite 判为非 CN 的域名";个人域名不在任何 geosite 分类里,自然不被劫持。帮助文案原写"非CN域名全劫持进代理"属过度承诺,已改正并指明正确做法(在 bot 里指到出口)。
- **`switch-core` 切换失败只报一句话**:实现把 python 的 stderr 直接丢弃,渲染抛异常 / 有出口无法转换 / 内核 check 不过三种失败挤成同一句;sing-box 方向同样。现在分别带出异常类型与信息、**逐个列出无法转换的出口名**、内核自己的报错;内核起不来回滚后附最近 15 行日志。

**安装 / 更新 / 换核的失败窗口**

- **`install.sh` 二进制事务**:备份 `cp` 失败仍被判成功、调用方也不检查 → 在没有可回退原件的前提下覆盖别人的二进制。现在备份失败即中止;备份走同目录临时文件 + SHA 校验 + 原子 `mv`;残留 `.pdg-preinstall` 与当前文件一致则自动清理,不一致则拒绝覆盖交人工。
- **半途失败不恢复**:`*_INSTALLED` 表示的是"装成功了吗",不能拿来表示"这次碰过目标没有" —— `install` 写了一半才失败时它还是 0,回滚就把半截二进制留在原地。改为事务台账(目标路径/装前是否存在/备份路径/装前 SHA),在**可能改动目标之前**记账,回滚按台账还原或删除并校验 SHA。
- **`PDG_FORCE_REINSTALL`**:覆盖重装失败时不再直接返回 —— 本次动过的二进制照样还原;且 `pdg` 不可用或安装前快照失败一律在覆盖任何文件**之前**中止。
- **内核热切**:备份 `cp` 不看结果、固定 `<svc>.prev` 会撞历史遗留文件 —— 实测备份失败 + 新核 check 失败时,还原那步会把**来源不明的历史文件**顶替成正在跑的内核。改为事务专属备份(`mktemp`)+ 记 SHA,备份不成立即中止;还原逐项校验 mv 成功 → SHA 一致 → 旧服务 active 且稳定。稳定判定改为窗口内多次采样并比对 `NRestarts`,识别"起来即崩"的崩溃循环。
- **`cmd_rollback` 校验快照配置**:改用**快照自带的内核**。拿刚升上来的新核去校验旧配置,新核不认旧配置时会把回滚自己挡住,而旧核和旧配置本该一起回去;旧快照确实不含内核时才退回当前内核并明确提示。
- **`pdg update` 不再"实际没完成却显示成功"**:`versions.sh` 读不到 / 下载失败 / SHA 校验失败 / 解压失败或产物为空,以前一律当作"保留现版本"后照报成功;现在版本与发布不一致时这些一律判失败并触发精确快照回滚。doctor 校验门也不再用 `|| true` 吞退出码、不再因缺 `jq` 整段跳过 —— 改用 python3 解析,要求 doctor 退出码为 0、输出为非空合法 JSON 数组、fail 数为 0,任一不成立即回滚。
- **iOS 平台组件**:`mitm_ca` / `mitm_server` / `mitm_wloc` / `probe81` / 描述文件模板原先全部 `|| true`,装失败就把上一版留在原地形成新旧混装。iOS 上改为必需件,任一失败即精确回滚;Android 仍不安装。doctor 侧补一道:WLOC 已启用却缺组件、或组件与当前发布 SHA 不一致,直接判 fail。
- **`install.sh` 回滚文案**:停服务 / 删 unit / `daemon-reload` / 删 nft 表 / 删配置目录的失败此前不计入,仍显示"已回滚到安装前状态"。现在各步骤仍相互独立(单项失败不挡后续)但全部计入,未全部成功只显示"回滚已尽力执行完…未能恢复"并返回非 0。

**诊断**

- doctor 新增「代理入口」检查:mihomo 模式下内网卡 80/443 必须 REDIRECT 到 redir 口,缺失即判 fail。补的正是此前的盲区 —— 该规则被误删后代理链路是断的,而防火墙检查只查"敏感端口有没有对全网开放",规则整条消失反而更"干净",一路判绿。sing-box 模式不适用则跳过,不误报。

## 2026-07-22 — v1.5.9(更新/回滚/安装回滚健壮性 + WLOC 严格预签 + Mihomo 诊断)

- **iOS GMS 清理**:迁移改为只从 nft **端口集**精确移除 5228-5230,保留整条 `{ 80, 443 } redirect`。旧实现按行删含 5228 的 redirect,而 mihomo 模板把三者写在同一条上,于是把 80/443 的代理入口一并删掉(线上已出现过);自定义形态识别不了就还原不动,交 doctor 提示。
- **`pdg update`**:关键步骤(git reset / 必需文件安装 / `__migrate` / 内核更新 / `daemon-reload`)失败即立刻回滚并返回非 0,不再吞掉失败照报「✅ 已更新」造成新旧混部;仅 iOS 组件、健康服务、certbot 钩子等可选文件保持容错。
- **内核热切**:确认新内核重启后稳定运行才删旧核备份 `.prev`(旧实现 check 一过就删,新核起不来便无核可退);配置 check 或重启不稳定都还原旧核并返回非 0。快照纳入两内核二进制,回滚不依赖联网重下。
- **跨内核回滚**:内核激活失败计入未恢复项 → 返回非 0 并打印「未完全回滚」+ 失败项,不再只 warn 后照报「✅ 已回滚」。
- **`install.sh` 回滚**:修 `MIHOMO_INSTALLED` 未初始化导致回滚在 `set -u` 下二次崩溃 —— 它会掩盖最初真正的安装错误,并让其后的 nftables / systemd-resolved / resolv.conf 还原全部没跑。状态变量在注册 EXIT trap 前全部初始化,判断一律带 `:-` 兜底,三项系统级还原各自独立(单项失败不中断后续),结束时明确输出「已回滚」或列出未恢复项;覆盖装前已有的内核二进制时还原原件而不是删掉。
- **WLOC**:叶子证书预签严格化(`prewarm(strict=True)` + 核对返回张数),少签一张即整体回滚,不再出现「证书没签出来却显示已启用」。
- **诊断**:`pdg report` / `pdg log` 的内核版本与日志按当前后端取(mihomo 机上不再固定去问 sing-box);doctor 敏感端口检查纳入 mihomo redir 口 7893(对全网开放判 fail,限内网来源放行)。
- **文案**:README 与 Bot 的 WLOC「手机端(全程用内网卡)」统一为同一套三步。

## 2026-07-22 — v1.5.8(健壮性修复一批 + Android/iOS 平台隔离 + README/文案重写)

- **健壮性修复**:MITM 叶子证书并发签发竞态(进程内 + 跨进程锁、随机序列号);`sb2mihomo` Hysteria v1 单独转 `type:hysteria`(不再误塞 hysteria2);`pdg update` 更新前快照失败即中止 + 按快照路径/Git SHA 精确回滚;WLOC 启停/切换/删除事务化(全量校验 + 失败回滚,不留脏态);`switch-core` 统一 systemd unit 生成 + 切核 enable/disable 纪律 + 跨内核回滚;CI mosdns 渲染补 `__HIJACK_SET_FILE__` 占位符 + fixture;`profile.env` 原子按键写(不再整覆盖丢 `PDG_TFO`/`PDG_HIJACK_MODE` 等);v1.4.x→WLOC 升级迁移(补 `force_hijack` 结构,幂等可回滚)。
- **平台隔离**:统一平台判定源 + 缺失回退提示;Bot 客户端菜单/状态/WLOC 按平台与内核门控(Android 不显示、不调用、不运行 iOS 功能;iOS 才有描述文件/WLOC/`pdg-probe81`);安装/更新按平台部署;GMS 5228–5230 仅 Android,iOS 精确清理;`sudo pdg ios` 仅 iOS。
- **TFO**:`PDG_TFO` 持久化在更新/重装后保留。
- **文案**:README 按结构重写(架构图含两内核、去宣传/绝对化措辞);`pdg update`/Bot 更新页明确「安装当前发布版指定并校验过的内核版本」;故障组、内核切换、WLOC 首次顺序文案统一。

## 2026-07-22 — v1.5.7(修 mihomo 下测出口对 direct 出口(jp)报"不通")

- **修**:mihomo 内核下「测出口」对 `direct` 出口(如 jp 直出)报「超时/不通」—— sb2mihomo 把 direct 出口映射成 mihomo 内建 `DIRECT`,clash_api 里没有该 tag 名 → `/proxies/jp/delay` 返 404。现测出口在 mihomo 后端把 direct 出口映射到 `DIRECT` 再查(显示仍用原名);回归进 test-panel。

## 2026-07-22 — v1.5.6(WLOC MITM 代码质量 + 文案微调)

- **改**:`_forward` 转发用 try/finally 兜 socket,异常路径不再泄漏 fd;TLS 上下文提到模块级复用(不每次转发重建、少重复加载 CA);`_dns_a` 加边界防越界(畸形响应返 None 不崩)。
- **文案**:WLOC「还原位置与隐私」步骤 → 「仅首次 / 后续无法改定位时」+「重启手机」;去掉「国内约1分钟/跨国几分钟」那句。README + bot 同步。

## 2026-07-22 — v1.5.5(修 mihomo 缺 TG 代理入站 + WLOC 使用文案)

- **修**:切到 mihomo 内核后 **Telegram 独立代理(:8445)不通** —— sb2mihomo 只渲染了 direct 入站(靠 nft REDIRECT),漏了 sing-box 的 `mixed` 入站(TG 代理),且 `inbound:[tg-proxy]→出口` 的路由被丢弃。现渲染成 mihomo `listeners`(mixed)+ `IN-NAME` 规则 pin 到其出口(空则跟 final)。真机 `mihomo -t` 校验通过。
- **文档**:README + bot 的 WLOC「日常使用」改成 4 步(控制中心关 WiFi → 关定位服务 → 首次/改不动时"还原位置与隐私"+重启 → 开定位服务)。

## 2026-07-21 — v1.5.4(WLOC 加地点更稳:直接发「名称 纬度,经度」也能加)

- **改**:iOS 下**裸发「名称 纬度,经度」**(如 `杭州 30.159222,120.16349`)直接加为 WLOC 地点,不必先点「➕ 添加地点」——避免 bot 重启丢了输入状态时加不上。

## 2026-07-21 — v1.5.3(doctor 按手机平台跳过不相关检查)

- **改**:`pdg doctor` / 自检按装机平台(iOS / Android)跳过不相关项——**安卓不再显示「MITM 插件」**(WLOC 仅 iOS),**iOS 不再显示「GMS 推送」**(5228-5230 是 Google/安卓推送,苹果走 APNs)。检查函数返回 `None` 即跳过,`checks.run()` 统一过滤。

## 2026-07-21 — v1.5.2(修从 v1.4.x 升级漏装 bot 模块 + README/WLOC 文案)

- **修**:旧版 `pdg update` 安装列表没有 `sb2mihomo.py` / `mitm_*.py`,从 v1.4.x **首次升级时(自更新时序滞后)这些新模块没装上** → `switch-core` 到 mihomo 报 `ModuleNotFoundError`、WLOC 也用不了。新增幂等迁移 `migrate_deploy_botfiles`(经 `pdg __migrate` 用新脚本跑),把 `deploy/bot/*.py` 全量部署到 `/opt/pdg-bot`,自愈首次升级 + 未来新模块。
- **文档**:README「组成」更新为**双核(sing-box / mihomo)+ WLOC**;bot WLOC 文案改成清晰的「3 步生效 + 等待时间」。

## 2026-07-21 — v1.5.1(修 `pdg update` 在 WLOC 启用时误回滚)

- **修**:iOS/WLOC 启用后,`pdg update` / `pdg rollback` 未重启 `pdg-mitm`,更新校验门的 doctor 判「pdg-mitm 未运行」→ 误判失败并回滚(且频繁 WLOC 切换可能已把 pdg-mitm 撞进 systemd start-limit 失败态)。现两处都 `reset-failed` + 按需重启 `pdg-mitm`。

## 2026-07-21 — v1.5.0(可选 mihomo 双核 + 一键切换内核 + 平台选择 + iOS 位置改写 WLOC)

本版把「流量内核」做成**可选双核**,并新增 iOS **网络定位改写(WLOC)**。

- **可选 mihomo 内核 + 一键切换**:装机 `PDG_CORE=mihomo`,或 bot / CLI **`pdg switch-core`** 在 sing-box ↔ mihomo 间**无损切换**(出口 / 分流 / 证书 / DoT / 规则全不动,只换内核 + 防火墙入站模型,失败自动回滚)。mihomo 无 sing-box 1.12 的版本天花板、可持续更新;`pdg update` 按 `lib/versions.sh` 钉死版本(SHA 校验)更新内核。新增 sing-box→mihomo 配置渲染层(sb2mihomo),覆盖各协议 / 组 / 规则集映射。
- **装机选平台(iOS / Android)**:一机一号,装机时选平台;iOS 才装 `pdg-mitm` 服务。
- **iOS 位置改写(WLOC,可选)**:把 iPhone 的**网络定位**改写到设定城市,**手机零 App**(系统 DoT + 信任网关 CA)。网关截 `gs-loc.apple.com` / `gs-loc-cn.apple.com`,**转发真 Apple 拿回真响应、只 patch 坐标**(格式 100% 保真,iOS 才接受)。bot「🍏 位置改写」管**多地点**(增删 / 一键热切换)。真机验证:内网卡 + 控制中心关 WiFi 下国内 / 跨国城市均可切。配方与限制见 README / [docs/design-mitm-plugins.md](docs/design-mitm-plugins.md)。
- **GFWList 劫持模式运行时切换**:`pdg hijack-mode` / bot 在「只劫被墙域名」与「全量」间切,SSH 到未被墙域名不被劫持。
- **修**:`pdg doctor` 限流(client_limiter)检查正则不容忍配置里的行内注释,导致全新安装误报 🟡;已修。
- `doctor` / `status` / `snapshot` / `rollback` / `update` / `uninstall` 全部 core-aware;新增 `pdg switch-core` / `pdg hijack-mode`。

## 2026-07-20 — v1.4.2(可选 GFWList 劫持模式)

- **增**:装机开关 `PDG_HIJACK_MODE=all(默认,行为不变) | gfw`,决定哪些海外域名的 A 记录被劫持到网关进代理。
  - `all`:非国内域名全劫持(原行为)。
  - `gfw`:只劫持 GFWList 里**真被墙**的域名;非墙的海外域名返真实 IP + AAAA → **SSH / 直连走域名不再被劫持到网关**(修此前"SSH 到域名被 DNS 劫持"的问题)。前提:内网卡 SIM 能直达一般互联网。
- 实现:`parse-geosite.py` 加抽 GFW 类目 → `geosite_gfw.txt`;mosdns 加 `hijack_set` 域名集 + 「不在劫持集的海外域名→真实解析」的门,`hijack_set` 文件按模式渲染。DNS 层,sing-box / mihomo 均适用。
- 已在真机 mosdns + 内网客户端实测 gfw 模式:非墙域名(example.com)返真实 IP、被墙域名(google)返网关、国内(qq.com)返真实 IP。
- 注:老装置运行时切换命令 + bot UI 为后续;当前装机时选 `PDG_HIJACK_MODE=gfw` 即可。

## 2026-07-20 — v1.4.1(修 TFO「开了却显示关闭」+ 快照/回滚加固)

- **修 TFO**:TCP Fast Open 开启后,只要再加一个出口(粘链接进来的不带 `tcp_fast_open` 标志),状态判断(原来靠「**所有**代理出口都带标志」推断)就被打成假 → 菜单显示「关闭」,且新出口也没真享受到 TFO。
  - 改为**持久化意图**(`profile.env: PDG_TFO`):`apply_sb` 每次改配置时,先按意图判定,再把它同步到**含新增的**所有出口 + 入站 —— 加/改出口不再冲掉 TFO 状态,新出口自动继承。`set_tfo` 失败时回滚意图,与配置回滚一致;老装(无 `PDG_TFO`)按现有出口标志回退推断,一次操作后即固化。
  - 回归:`tests/test-tfo.py`(持久化意图 / 加出口不冲掉 / 老装兼容)+ 接入 CI。
- **快照/回滚加固**:临时目录非空校验(避免空路径退化到 `/etc`);解包用 `tar --no-recursion` 按归档清单落树,不隐式递归顶层父目录、不误改 `/etc`·`/opt` 元数据;面板临时态净化改为**同目录 mktemp + 原子 `mv`**(600 权限,失败不留半成品),归属判定加 `external_ui_download_url` 校验。

## 2026-07-14 — v1.4.0(观测面板加固:事务/归属/净化/完整性)

面板准确定位为「临时观测/控制面板」(zashboard 原生可看流量并断开连接;持久规则/出口仍走 bot)。系统性加固:

- **事务与回滚**:开启只在配置、服务、防火墙全部成功后才报开启;任一步失败回滚到关闭态。防火墙操作有返回值并复核最终规则。链接发送失败立即关面板。
- **配置归属(off/on/custom 三态)**:精确识别项目默认关闭态 / 项目管理开启态 / 用户自定义;**自定义 `clash_api` 一律不接管、不覆盖、不清理**。
- **备份/恢复净化**:bot `backup_blob` 与 CLI `pdg snapshot`/`rollback` 都把面板临时态(公网监听 + 密钥 + UI)洗成关闭态再入档/落盘——**修复了备份会泄露面板密钥**;自定义 clash_api 不动。
- **计时状态机**:会话代号隔离旧回调,过期定时器不能关新会话/删新链接;重新计时删旧链接;关闭失败保留链接并 60 秒重试(不假报已关);手动/自动/启动清理共用同一关闭事务。
- **zashboard 完整性**:版本与 SHA 从 `lib/versions.sh` 单一来源读取;下载校验 + zip-slip 防护 + 内容指纹自愈(本地被篡改下次开启恢复固定版本)+ 原子替换;`external_ui_download_url` 固定到项目资源。
- 文案与 CLI 状态如实反映临时开放态。

## 2026-07-14 — v1.3.2(启动兜底清残留放行规则)

- **修**:面板自动关闭若在改配置后、撤防火墙前被打断(如 bot 崩溃/被杀),会残留一条内网 9090 放行规则,而原启动兜底只在配置态为「开」时清理、查不到它。启动时现在也会清掉「配置已关但仍存在」的 9090 放行规则。

## 2026-07-14 — v1.3.1(观测面板定时自动关闭)

- **增**:观测面板开启时可选**自动关闭时长**(10 分 / 30 分 / 常开)。到点自动关面板 + **删掉含密钥的链接消息** + 通知——忘了关也有暴露上限。手动「🔒 关闭」同样会删链接。
- **兜底**:bot 重启后若发现面板仍开着(上次的定时器已随进程丢失),启动时直接关闭,避免长期暴露。

## 2026-07-14 — v1.3.0(观测面板 zashboard,bot 开关控制)

- **增**:bot「🛠 运维 → 📊 观测面板」——一键开启 [zashboard](https://github.com/Zephyruso/zashboard)(看连接/流量/延迟/日志、右键测速)。**只观测,改配置仍走 bot**。
- 默认**零暴露**:clash_api 只绑 `127.0.0.1`、无 secret、防火墙不放行 9090。开启时临时绑 `0.0.0.0` + 随机 secret + `external_ui`(sing-box 自身托管面板静态文件,**不新起服务、几乎不占内存**)+ 放行**仅内网卡段**→9090,并发一条 zashboard 一键链接(`#/setup` 自带 host/port/secret);关闭撤销全部。
- zashboard dist 钉版本(`v3.15.0` dist-no-fonts)+ SHA256 校验(供应链);bot/doctor 的 clash_api 调用自动带 `Authorization: Bearer`(面板开启后本机调用也要鉴权)。
- ⚠️ HTTP 明文、链接含密钥(别转发)、暴露一个内网端口 —— 看完请关闭。

## 2026-07-13 — v1.2.5(快照/journald 迁移边界收口)

- **修**:`run_all_migrations` 会删掉历史错路径文件,而 `cmd_snapshot` 随后无条件打包该路径 → tar 报 `Cannot stat` 返 2,却仍提示"✅ 快照"。改为只打包**存在的**路径并检查 tar 返回值。
- **修**:journald drop-in **补建/重建分支**的重启用 `restart || true`,重启失败仍报绿。改为检查重启结果,失败给 warn。
- **修**:v1.2.3 拼接畸形留下的 `[Journal]SystemMaxUse=…`(段头与 key 同行)会被前缀正则误判为已有段头,修不掉;只有 key、完全无段头的文件也不修。现要求**独立**段头(整行 `[Journal]`),缺失/畸形则按标准内容**重建**(项目独占文件,安全)。

## 2026-07-13 — v1.2.4(迁移即时生效 + journald 迁移稳健性)

- **修**:`pdg update` / TG Bot 更新仍在旧 Bash 进程里跑,新版迁移要等下次 root 命令才生效。`cmd_update` 装好新脚本后改为 `bash /usr/local/bin/pdg __migrate` 用**新脚本**跑迁移(迁移集中到 `run_all_migrations`)。
- **修**:`pdg snapshot/rollback` 未含 journald drop-in——强制重装失败后无法真正还原封顶。快照现纳入正确+历史错路径的 `50-pdg.conf`,回滚后 `restart systemd-journald`(`CanReload=no`)。
- **修**:mosdns cache 迁移失败(生成/复核/mv)会 `return` 连带跳过后面的 journald 修复。拆成 `_migrate_mosdns_cache` / `_migrate_journald_cap` 两个独立函数,mosdns 失败也不影响 journald。
- **修**:journald 写入失败(只读目录)仍报绿——`_journald_set_key` 现传播写失败(返回 2),迁移改后复核 **System 与 Runtime 两个值**并检查 journald 重启结果,任一异常给 warn。
- **修**:零字节 / 末尾无换行的现有 drop-in 文件会生成缺 `[Journal]` 段头或 `[Journal]SystemMaxUse=…` 拼接的非法配置;追加时补段头与换行。

## 2026-07-13 — v1.2.3(journald 封顶稳健性 + RuntimeMaxUse)

- **修**:低内存迁移写 journald 封顶只会替换已有的 `SystemMaxUse=` 行——文件没有该有效行时 `sed` 空替换却"假成功",只有 `#SystemMaxUse=` 注释行时又被 `grep` 误判为已存在而跳过。改为只认**未注释有效行**:缺则追加、有则替换,写后复核实际值。
- **增**:模板与迁移同时维护 `RuntimeMaxUse`(管 `/run/log/journal` 易失日志),与 `SystemMaxUse`(管 `/var/log/journal` 持久日志)一起封顶,覆盖两类系统。
- **修**:卸载 / 安装失败回滚现在同时删正确路径与历史错路径的 `50-pdg.conf`,并 `systemctl restart systemd-journald`(Debian 12 该服务 `CanReload=no`,不重启封顶不会松开)。

## 2026-07-13 — v1.2.2(journald 迁移补齐: 清错目录残留 + 补建正确目录)

- **修**:v1.2.1 只改「正确目录已有的」journald 文件;但用旧版 install.sh 装的机器,文件在**错目录**、正确目录为空,迁移会跳过 → 封顶仍不生效。`migrate_lowmem` 现在会清掉错目录(`/etc/systemd/system/journald.conf.d/`)的历史残留,并在正确目录缺文件时按内存模式补建(标准 50M / 低内存 20M)。回归加进 `test-lowmem.sh`。

## 2026-07-13 — v1.2.1(修 journald drop-in 路径)

- **修**:journald 用量封顶(`50-pdg.conf`)一直被装到 `/etc/systemd/system/journald.conf.d/`——但 journald 实际只读 `/etc/systemd/journald.conf.d/`,导致封顶(标准 50M / 低内存 20M)从未生效。install.sh / uninstall.sh / pdg.sh(低内存迁移默认路径)统一改到正确目录;老装 `pdg update` 后触发迁移即把已有的正确路径文件调到目标值。此前 pre-existing,借低内存模式一并修正。

## 2026-07-13 — v1.2.0(mosdns 限流 + 低内存模式 + bot 凭据保护/并发)

- **增 · mosdns 单客户端 QPS 兜底**:模板加 `client_limiter`(qps200/burst400/mask4-32/mask6-128),internal_sequence 缓存前 `!$client_limiter → reject 5`,仅内网来源生效,超限 REFUSED。定位为误配置/异常设备兜底,非 DDoS 防护;nftables 来源限制仍是第一道边界。老装幂等迁移;doctor 精确校验参数+动作(缓存前 reject 5)。
- **增 · 克制版低内存模式**:`PDG_LOWMEM=auto|1|0`,MemTotal ≤ 1300MiB 判低内存,持久化 `profile.env`(auto 沿用已有选择)。只调 mosdns cache(8192/2048)+ journald 上限(50M/20M);**不加 sysctl、不自动 swap、不设 MemoryMax/GOMEMLIMIT**。install 渲染 + 老装幂等迁移(临时文件+校验+原子替换+失败不重启);`pdg status`/doctor 显示内存模式与 cache size。
- **增 · bot 凭据保护 + 并发**:粘贴节点链接后立即清状态 → 独立线程尽力 `deleteMessage` 删含凭据原消息(BUSY/提交失败路径也删)→ 成功只显示出口名,失败不回显链接/异常正文;后台有界执行器不阻塞主轮询;per-chat BUSY 防重复;`apply_sb` 用进程内非阻塞锁 + 跨进程 flock(复用 `/run/privdns-gateway.lock`)与 `pdg update/rollback` 协调,并做事务回滚(check/重启异常也还原备份、不留未验证配置)。
- 全程新增回归测试并挂 CI(限流迁移+真限流、低内存 fixture、凭据/并发/事务),sing-box 模板零改动。

## 2026-07-03 — v1.1.16(doctor 宽区间泄露判定 + GMS 启用自检)

- **修**:doctor 防火墙检查对端口区间原先"最多展开 16 个端口",`tcp dport { 1-65535 } accept` 这类误配置会漏报;改为**判断敏感端口是否落在区间内**(不枚举),多宽的区间都报得全。
- **增**:doctor 新增「GMS 推送」自检(warn 不 fail):只读检查 sing-box 5228-5230 入站与防火墙内网放行是否齐全——老装第一次 `pdg update` 跑在旧脚本里,GMS 迁移要等下一次 root 管理类命令才触发,该项把这个空窗显性化并给出指引;自定义防火墙用户按提示手动放行即可。
- 回归:宽区间全网开放须报全敏感端口/限内网不报;`check_gms` 四种形态全打桩测试。

## 2026-07-03 — v1.1.14(GMS/FCM 推送端口 5228-5230)

- **增**:支持 Google 全家桶的推送/登录原生通道 `mtalk.google.com:5228-5230`——防火墙对内网卡来源放行 + sing-box 加三个嗅探入站,mtalk 照常按域名分流。此前该域名被 DNS 劫持到网关但端口静默丢弃,客户端傻等 TCP 重传后才回落 443;回落慢的手机(部分 ROM/旧版 GMS)表现为「Google 服务连不上」。真机抓包实锤:GMS 对 5228 同一连接重传 5 次、白等 15 秒。
- **老装置自动迁移**(任意 `pdg` 管理命令触发,幂等):`migrate_singbox_gms` 补入站(check 不过/起不来自动还原)、`migrate_fw_gms` 只改**原装形态**的端口集行(自定义防火墙不碰,提示手动加;备份+nft -c+失败还原)。
- doctor 防火墙检查:泄露检测覆盖 5228-5230,且能解析端口区间写法。
- 回归 `tests/test-gms-migration.sh` 进 CI(原装补/自定义跳过/幂等/双重回滚);功能测试加「GMS 端口入站 + mtalk SNI 分流」真实用例。
- 云厂商安全组注意:**控制台防火墙也要放行 5228-5230(TCP,内网卡段)**,见 INSTALL 端口表。

## 2026-07-02 — v1.1.13(返回菜单清输入态)

- **修**:点「🏠 主菜单」或「⬅️ 返回 xx」只翻页**不清待输入状态**——比如点「📱 iOS 描述文件」后按返回,下一条随手发的文字仍会被当成 SSID 名单生成描述文件。现在 `menu`/`status`/`nav:*` 回调统一清掉 `state`(待输入)和 `del_sel`(删规则勾选)。
- 回归断言加进 `tests/test-bot-nav.py`(动态: 六种返回路径后状态/勾选必须为空)。

## 2026-07-02 — v1.1.12(iOS Wi-Fi 探测判定 + SSID 直连名单)

- **改**:iOS 描述文件的 Wi-Fi 策略从「一律不启用 DoT」改为**和蜂窝一样按 `:81` 探测判定**——只有出网路径能以内网段源 IP 摸到网关的 Wi-Fi(路由器走专线/插内网卡)才启用;普通宽带/带分流代理的 Wi-Fi 探不通 → 自动直连,与 Wi-Fi 自带的代理互不叠加。
- **增**:bot「📱 iOS 描述文件」生成前可发 SSID 名单(每行一个,最多 8 个)把指定 Wi-Fi **强制直连**——`SSIDMatch` 规则插在探测之前,治 captive portal 假 200 误判;点「⏭ 直接生成」跳过。SSID 用 plistlib 注入,含 `&<>` 等字符不破 XML。
- `pdg ios`(CLI 二维码)零改动获得新 Wi-Fi 探测行为;安卓私密 DNS 是系统全局开关,无按 SSID 条件启停,项目层面无法支持。
- 回归 `tests/test-ios-ssid.py` 进 CI(规则顺序/探测 URL/SSID 插入/特殊字符)。

## 2026-07-02 — v1.1.11(出口真改名)

- **增**:bot「出口管理」加「✏️ 改名」——真改 outbound 的 tag 并**级联更新所有引用**:分流规则(含 TG 出口规则)、故障组成员、默认出口(final)、规则集元数据里的目标出口,不用再"删了重加"。
- 改名走 `apply_sb`(check 失败自动回滚);direct 出口(模板锚点、WDA 依赖其 tag)不可改;重名/保留字/非法字符(清洗后无字母数字)拒绝且不动配置。
- 规则集改名维持 label 覆盖不变:`rs_xxxx` 是内部哈希 tag,用户不可见,真改 tag 只增风险无收益。
- 回归 `tests/test-exit-rename.py` 进 CI;两台真机已用真实出口做过"改名→级联校验→改回,前后配置逐项一致"的往返测试。

## 2026-06-27 — v1.1.10(删规则集结果显示名 + 清死代码)

- **修**:删除规则集后的「已删除规则集 rs_xxxx」**没用改过的显示名**;`del_ruleset` 现在删前取 `label` 用于结果消息(回归断言加进 `test-domain-label.py`)。
- 删掉成了死代码的 `exits` 回调(子菜单已改用 `exit_list`,无人再指向 `exits`)。
- 把漏挂的 `test-maintenance-polish.py` 补进 CI。

## 2026-06-27 — bot TFO 页面补主菜单

- bot「🚀 TFO」页面补上「🏠 主菜单」按钮,与 DNS 上游等运维三级页保持一致。

## 2026-06-27 — bot 更新/DNS 按钮与点击响应优化

- bot「🔄 更新」检查中的“检查更新中…”不再铺一级菜单,改为只显示返回主菜单按钮。
- bot「🌐 DNS 上游」页面补上「🏠 主菜单」按钮,与其它三级页一致。
- Telegram callback 先停止按钮转圈,再执行检查更新/测出口/自检等较慢动作,降低“点了不跟手”的体感延迟。

## 2026-06-27 — bot 测域名显示名与测出口键盘修正

- bot「🔎 测域名」命中规则集时改为显示用户改过的规则集名称,不再暴露内部 `rs_xxxx`。
- bot「🚦 测出口」等待中的“测试中…”不再铺一级菜单,改为只显示返回主菜单按钮。
- CI 增加测域名规则集显示名回归。

## 2026-06-27 — bot 三级返回与自检端口补齐

- bot 出口管理/分流管理里的三级页面与操作结果改为返回对应父菜单;一级「测出口」仍返回主菜单,避免复用 callback 造成跳错层级。
- 自检防火墙口径补上 Telegram SOCKS5 代理端口 `8445`,与 nftables 模板里的 `53/80/81/443/853/8445` 保持一致。
- CI 增加 bot 三级导航与自检防火墙端口回归。

## 2026-06-27 — 发布/更新链路补齐 + bot 运维结果返回按钮

- `install.sh` 自举也切到最新 `v*` 发布 tag,不再把 `/opt/privdns-gateway` 种成 main 的浅克隆;本地 clone 运行安装时也会重进最新发布 tag。
- `pdg update` 与 bot 更新检查对老浅克隆先 `fetch --unshallow --tags`,并区分 `merge-base` 的“不落后”和 git 错误,避免把浅历史异常误报成已最新。
- 安装文档清掉 sing-box `1.12.9` 残留,改成当前锁定版 `1.12.25` / 项目锁定版表达。
- bot 运维动作结果(TFO/重启/更新规则库,WDA 切换)改为返回运维/DNS 上游的小键盘,不再直接铺整个一级菜单。

## 2026-06-24 — v1.1.2(修「改出口」列表显示规则集旧名)

- 分流管理「✏️ 改出口」选规则的列表里,**规则集规则原先显示内部 tag `rs_xxxx`,没用你改过的显示名**;现 `editable_rules` 查 `_rs_meta` 的 `label`(没有才回退 `rs_xxxx`),与「📋 规则」列表一致。
- (说明:两个同地区节点想"自动测延迟、谁快用谁 + 故障切换",用「🔀 新建故障组」(urltest),不是规则集——规则集是域名集合。)

## 2026-06-24 — v1.1.1(`pdg update` 只跟发布 tag)

- `pdg update` / bot『🔄 更新』从「跟 `main` 最新提交」改为「**更新到最新发布 tag**」(`v*`,按版本号降序取最高)。外部用户只会拿到打了 tag 的发布版,不再拉到 main 上未发布的中间提交;仓库没 tag 时中止并提示。
- `--dry-run` 与更新检查显示「当前 vs 最新发布 tag」及其间提交;`reset --hard <tag>`。
- 适合「对外发布走 tag、main 随时迭代」:你推 main 不影响别人,打 tag 才算发布。

## 2026-06-24 — v1.1.0(版本显示改 git describe)

- `pdg status` 的「代码版本」与 bot『🔄 更新』检查里的「当前」从 commit hash 改为 **`git describe --tags`**:在 tag 上显示 `v1.1.0`,领先 tag 则 `v1.1.0-N-g<hash>`。`pdg update` 与检查更新的 `git fetch` 加 `--tags`,确保各机能拿到 tag 供 describe。
- **打 tag `v1.1.0`**。自 v1.0.0 起累计:WDA 流媒体/服务解锁开关、出口多协议解析(hysteria2/tuic/vless-reality/anytls/socks5/http + Surge ss 行)、防火墙独立表 `inet pdg` + 幂等迁移、mosdns 多厂商上游 + `concurrent` 故障转移、DNS 层/解析/迁移多项回归测试入 CI、sing-box 锁定 1.12.25,以及大量评审加固。

## 2026-06-24 — sing-box 锁定版升到 1.12.25(1.12.x 最高补丁版)

- **`lib/versions.sh`: `SINGBOX_VER` 1.12.9 → 1.12.25**(当前 1.12.x 最高;仍是 1.12.x,`sniff_override_destination` 在,**不碰 1.13**)。同步更新 amd64/arm64 SHA256。十几个补丁版的 bug/安全修复。
- 两台线上对齐到 1.12.25(`pdg update` 不动二进制,手动换 + 校验 SHA256 + 重启 + 回滚兜底);新装直接装 1.12.25;CI 的 schema 校验也对 1.12.25 跑。

## 2026-06-24 — 出口支持更多协议链接(hysteria2/tuic/vless-reality/anytls/socks5/http)

- **bot「📤 出口管理 → 添加」新增解析**:`hysteria2://`(含 sni/insecure/obfs)、`tuic://`(uuid:pass、alpn/congestion_control)、`anytls://`、`socks5://`、`http(s)://`,并**扩展 `vless://` 认 Reality**(`pbk`→`reality.public_key`、`sid`→`short_id`、`fp`→`utls.fingerprint`、`flow`)。`PROXY_TYPES` 同步扩容,新协议出口可正常选默认/进故障组/测出口/删除。
- **修 gRPC 服务名**:`_transport` 现从 `serviceName` / `service_name` / `path` 三者取 grpc service_name(原先只看 `path`;vless/vmess 的 grpc 分享链接多用 `serviceName=`,会丢导致连不上)。
- **验证(两层、都进 CI)**:`tests/test-parse-links.py` 断言各协议字段映射(含 gRPC serviceName);`tests/test-outbound-schema.sh` **下载项目锁定版 sing-box(`SINGBOX_VER`=1.12.9、钉死 SHA256)对 parse_link 生成的全部出站跑 `sing-box check`**——只测解析 dict 不够,字段名要跟锁定版 schema 对得上(常随版本小变)。⚠️ 连通性仍需各自拿真实节点测。
- 仍走手写 config 的:`shadowtls`(无标准链接)、`ssh`(无链接)、`wireguard`(1.12 是 `endpoints`)。

## 2026-06-24 — 出口支持粘贴 Surge 的 ss 行

- **「📤 出口管理 → 添加」除 `ss:// / vmess:// / trojan:// / vless://` 链接外,也认 Surge 代理行**:`名字 = ss, 服务器, 端口, encrypt-method=…, password="…", tfo=true, udp-relay=true`(`encrypt-method`→method、`tfo=true`→`tcp_fast_open`;SS2022 如 `2022-blake3-aes-128-gcm` OK;udp-relay 是 sing-box ss 出站默认行为)。其它类型仍用对应 URI。
- 加 `tests/test-parse-links.py`(进 CI):Surge ss 行 / `ss://` SIP002 / 非法输入 三类断言。
- **文档澄清**:README/QUICKSTART/forum-post 说明出口**协议 = sing-box 全部出站**;`ss://vmess://trojan://vless://` + Surge ss 行是 bot 能直接粘的,其它(hysteria2/tuic/vless-reality/shadowtls/anytls/ssh/socks/http/wireguard 等)手写 `config.json` 即可——避免误以为只支持这四种。

## 2026-06-24 — 流媒体/服务解锁开关(WDA)

- **bot『🌐 DNS 上游』新增解锁开关**:两个按钮在「🛬 解锁走落地出口」与「🔓 解锁走 WDA」之间整体切换。
  - 🔓 WDA:一批可解锁的服务域名(Netflix/Disney+/Prime/AppleTV/YouTube/Dazn/U-NEXT/iQiyi/TVBAnywhere/DMM + OpenAI/Claude/Gemini + Steam 等)整体 → **jp 直出**(从 VPS 被授权 IP 出)+ 经 mosdns 用**解锁 DNS `22.22.22.22`** 解析到中继。其余流量照常分流。
  - 🛬 落地:撤掉规则,这些域名回落各自现有出口(hk/tw)。
- **mosdns 加常驻"解锁支"**(平时休眠):`unlock_upstream`(22.22.22.22, concurrent 1) + `geosite_unlock`(读 `unlock.txt`) + main_sequence 一条「**本机查询**命中解锁域名 → 解锁 DNS」的支(带 `jump has_resp`,否则答案会被 `remote_upstream` 覆盖——实测踩过)。只对 sing-box 直出的本机查询生效,手机劫持路径不变。
- **开 WDA 前自检授权**:点 🔓 时先探测解锁 DNS 是否对本机返回中继(本机 IP 已在服务商后台加白),**没授权就拦下并提示去后台授权本机 IP**——避免"没授权却开 WDA → 拿不到中继、流媒体反而挂"。DNS 上游页也直接显示要授权的本机 IP。docs/INSTALL.md 加「流媒体/服务解锁(WDA)」节。
- **修:关 WDA 现在会清空 `unlock.txt`**。原先点 🛬 只撤 sing-box 规则、没清 mosdns 的 `unlock.txt`,导致"落地模式下本机解析这些域名仍走解锁 DNS"的残留(与配置注释"落地时 unlock.txt 留空"不符)。现 `set_wda_mode(False)` 撤规则后清空 `unlock.txt` 并重启 mosdns(解锁支彻底休眠);`_write_unlock_file([])` 可写空。dns-policy-test 加「空 unlock.txt → 解锁域名回落普通上游」回归。
- **旧装自动迁移** `migrate_mosdns_unlock`(随管理类 pdg 命令幂等补该支);install 建空 `unlock.txt`(空=休眠,不改现有行为)。
- **测试**:dns-policy-test 加「解锁域名经本机 → 解锁 DNS(非普通上游)」断言,正好回归 `jump has_resp`。
- 说明:解锁地区取决于厂商面板选的平台(VPS 在日本→JP 平台→日本区);解锁的价值在于**中继是干净 IP**,避开 Netflix 对机房 IP 的代理封锁。

## 2026-06-23 — 评审第九轮:concurrent 也给旧装自动迁移 + 单上游不重复查

- **旧装升级自动补 `concurrent`**:`pdg update` 不重渲染 `/etc/mosdns/config.yaml`,旧装升上来仍是默认随机单上游、无故障转移。新增 `migrate_mosdns_concurrent`(随管理类 `pdg` 命令幂等触发):只给**缺 concurrent** 的 forward 块补该字段、**不动用户现有上游**;备份 `cmp` 校验、重启 mosdns 失败自动还原。
- **单上游不再被查两次**:bot 与迁移都按**上游数定值**——单上游 `concurrent: 1`(否则 mosdns 取模会对同一台并发发两次相同查询)、≥2 才 `concurrent: 2`。
- 回归测试 [tests/test-mosdns-concurrent.sh](tests/test-mosdns-concurrent.sh)(进 CI):多上游→2 / 单上游→1 / 已有不动 / 二次幂等 / 上游顺序保留。

## 2026-06-23 — 评审第八轮:多上游真故障转移 + 测试去假阳性 + 探测发真查询

- **多上游=真故障转移(`concurrent: 2`)**:mosdns `forward` 默认 `concurrent=1` = **随机选 1 个上游、出错不换下一个**,多写上游并不会自动转移(查到挂的就直接失败)。两个 forward 块都显式设 `concurrent: 2`(并发查随机起点的 2 个、先返回的有效结果胜、出错的跳过)→ 任一上游挂掉查询仍成功。bot 改上游时也强制保留 `concurrent: 2`(否则退回默认 1,多上游形同虚设)。新增「一台上游故障仍可解析」回归用例(实证:改回 `concurrent:1` 该用例即失败)。
- **DNS 测试去假阳性**:`mock_dns.py` 现对 AAAA/HTTPS 返回**真实非空记录**——这样「代理域名 AAAA/HTTPS 被置空」断言验证的才是 mosdns 抑制逻辑(原先 mock 本就空,删掉抑制也会假阳性通过);并新增「国内域名 AAAA **不**被置空」断言。
- **上游探测发真实 DNS 查询**:`pdg doctor --deep` 的「DNS 上游探测」DoH 改为 POST `application/dns-message` 真查询并校验 HTTP200+应答(ID/RCODE/有回答),DoT 改为 TLS 握手 + DNS-over-TCP 查询并校验应答——不再「任意 HTTP 码 / 仅 TCP 连通」就算健康(CDN/反代/错服务占端口会被识破)。

## 2026-06-23 — 锦上添花:国内多厂商上游 / DNS 层功能测试 / 上游可观测

- **国内上游默认多厂商冗余**:`local_upstream` 默认加 `udp://119.29.29.29:53`(腾讯),与阿里 DoH+UDP 一起,阿里抽风/限速时异厂商顶上。想换/加仍用 bot『🌐 DNS 上游』。
- **DNS 层功能测试** [tests/dns-policy-test.sh](tests/dns-policy-test.sh)(并入 CI):真起 mosdns + 渲染真实 `config.yaml`,本地 mock 上游,断言「DNS as policy」核心——内网来源:代理域名 A 劫持到网关 IP、AAAA/HTTPS 置空、国内域名直连;**非内网来源不劫持**(按 `client_ip` 门控)。补上了此前只测流量层(SNI 分流)、DNS 层无测试的空白。
- **DNS 上游可观测性**:`pdg doctor --deep` 新增「DNS 上游探测」——逐个上游测可达性/延迟(UDP/TCP 发真实查询、DoH/DoT 测连通)、报每组 N/M 与最慢者,并统计**近 1h mosdns 上游错误次数**;整组不可达记 fail、部分挂记 warn。

## 2026-06-23 — 评审第七轮:原装识别改严格白名单(默认拒绝),收口

- **修漏检**:第六轮的语义判断只深查含 `dport` 的行,`ip saddr X accept` / `tcp accept` / `counter drop` 这类**不含 dport 的自定义规则会漏过**被当原装 → 迁移时静默删除。
- **改严格白名单(默认拒绝)**:`_fw_is_stock` 现要求去注释后**每一行都必须匹配某条已知原装规则**(用正则,兼容 forward/output 单行/多行、各年代端口子集);出现任何不认识的行即判自定义、跳过迁移。用全部 6 个历史模板(`144c865`~`8107b7f`,含 853 曾对全网开放、UDP 443 曾放行等早期形态)+ 真机备份验证均判原装;自定义一律跳过。
- **回归测试扩到 14 例**(6 原装变体 + 8 自定义,含上述三类无 dport 规则),并入 CI。

## 2026-06-23 — 评审第六轮:修第五轮"原装识别"误判 + 回归测试

- **修 bug**:第五轮的 `_fw_is_stock` 用逐行精确白名单,只认多行写法的 forward/output 链;而**真实老模板**(`62443ad`~`8107b7f`)的 forward/output 是**单行写法**,端口集也随年代不同({53,80,81,443}→+853→+8445)。结果**所有真·原装老配置都被误判为"自定义"**,自动迁移与 `pdg migrate-fw` 全部跳过 → 老机器永不迁移。
- **改为语义判断**:不再挑排版。只要"没有别的 table、hook 仅 input/forward/output、无 NAT 改写、无原装不用的匹配维度、每条 dport 规则的端口/来源都在允许范围"即判原装;单行/多行、各年代端口子集都正确识别;真有自定义(额外端口/额外表/额外来源/对全网开端口/转发规则)才跳过。
- **加回归测试** [tests/test-fw-migration.sh](tests/test-fw-migration.sh) 并入 CI:覆盖 4 种原装变体(含曾误判的单行写法)+ 5 种自定义,断言"原装应迁移、自定义应跳过";已验证它能抓到本次 bug。

## 2026-06-23 — 评审第五轮:迁移不静默丢用户自定义规则

- **迁移前先认"原装"**:`migrate_firewall_to_pdg` 用标准模板重建只保留 SSH端口+内网段,会丢掉用户在旧 `/etc/nftables.conf` 里手加的端口/规则/额外表。现新增 `_fw_is_stock` 白名单校验:**只有逐行全部落在已知原装行内才自动迁移**;检测到自定义端口/规则/别的 `table`(如 NAT)就**跳过并提示手动并入**,旧配置原样不动(hook/doctor 兼容旧表,不迁也能用)。

## 2026-06-23 — 评审第四轮:备份完整性 / 只读命令语义

- **迁移前确认备份完整才覆盖**:`migrate_firewall_to_pdg` 现 `cp` 后用 `cmp -s` 逐字节校验备份;备份失败/不完整(磁盘满)即**中止迁移、不动现网**;写新配置同样校验,失败则用已验证的备份还原。杜绝"备份没成、却已覆盖/截断 `/etc/nftables.conf`"。
- **只读命令不再触发迁移**:自动迁移只在**管理类**命令(`update`/`restart`/菜单等)触发;`status`/`doctor`/`log`/`traffic`/`report` 保持"只读不写"。只跑只读命令的可显式 `sudo pdg migrate-fw`(且不迁也能用:hook/doctor 兼容旧表)。[docs/INSTALL.md](docs/INSTALL.md) 增「升级」节说明。

## 2026-06-23 — 评审第三轮:迁移自愈 / 守卫删表 / active 防竞态

- **首次升级也能自动迁移**:`pdg update` 自更新时,当前进程跑的还是旧脚本、不会调用新迁移逻辑(要等下一次)。现新版 `pdg` **每次以 root 运行任意子命令时都幂等自检并迁移**(已迁移则首个 grep 秒退);另加显式命令 `sudo pdg migrate-fw`。
- **迁移加载失败绝不删旧表**:`migrate_firewall_to_pdg` 现 **只有 `nft -f` 成功且确认 `inet pdg` 已在内核** 才 `delete table inet filter`;失败则还原 on-disk 配置、保留旧表(`nft -f` 原子失败不改内核)→ 不会出现"新表没载入、旧表已删、防火墙消失"。
- **`active` 检查防竞态**:`_svc_active` 改为**要求连续多次保持 active**(flapping 的 failed/activating 会打断连击),不再"瞄到一次 active 就放行";安装的服务门同样改为连续 3 次保持。规则集回滚后**先确认旧服务恢复再删 `.bak`**,连旧档都起不来则保留 `.bak` 备查。

## 2026-06-23 — 评审第二轮:升级迁移 / 安装事务性 / 重启校验

- **`pdg update` 自动迁移旧防火墙**:老机器升级后,把旧的 `flush ruleset` + `table inet filter` 迁到独立表 `inet pdg`(解析旧配置里的 SSH 端口/内网段 → 渲染新模板 → `nft -c` 校验 → 备份 → `nft -f` → 删旧表,全程 SSH 不断、幂等)。不迁移则证书续期 pre-hook 进不了 `inet pdg`、开不了 80。
- **两种表名都兼容**:证书 pre-hook 与 `doctor` 的防火墙检查现同时认 `inet pdg`(新)和 `inet filter`(旧未迁移),避免老机器续期开不了 80 / 自检误报"读不到防火墙"。
- **已有部署不再用 install.sh 覆盖**:检测到既有部署时 `install.sh` **直接拒绝并引导 `pdg update`**(带快照+回滚);确需原机重装的显式 `PDG_FORCE_REINSTALL=1`,此时先打快照,失败用 `pdg rollback` 恢复。修掉了"已有部署回滚实为空操作、配置却已被改写"的问题。
- **安装成功门后移**:`systemd` 默认 `Type=simple`,`systemctl start` 返 0 不代表进程没随即崩溃。安装收尾改为**确认 mosdns/sing-box/probe81 真的 `active`** 才置"提交点",否则打印日志并触发回滚——不再"服务没起来也报装好"。
- **规则更新重启失败兜底**:`refresh_rulesets` 改为**重启 → 确认 `active` → 再删 `.bak`**;起不来则还原旧规则集并重启,不会断网后无可回滚。`apply_sb` 同样补 `is-active` 复核(同 `Type=simple` 隐患)。

## 2026-06-23 — 供应链/事务性/真功能测试(社区评审·可选项)

- **二进制 SHA256 校验(供应链)**:`install.sh` 下载 mosdns / sing-box 后,先比对**钉死的官方 SHA256**(amd64+arm64)再安装,不符即 `die` 拒装。版本号与 4 个哈希集中到单一可信源 [lib/versions.sh](lib/versions.sh),`install.sh` 与功能测试共用。
- **事务性安装·失败自动回滚**:`install.sh` 加 `trap … EXIT`,中途失败时——**全新安装**:停并清掉本次铺的单元/配置/二进制、`nft delete table inet pdg`、还原 `nftables.conf` / `resolv.conf` / `systemd-resolved` 到装前;**既有部署上升级失败**:不动其服务/配置/二进制(避免误伤),提示用 `pdg doctor` / `pdg rollback`。成功到防火墙应用后置"提交点",此后只剩打印、不再回滚。
- **真功能测试(非静态)**:新增 [tests/functional-test.sh](tests/functional-test.sh)——真起 sing-box(direct 入口开 sniff,与生产同款),用 3 个本地 mock SOCKS5 当出口,按不同 **TLS SNI** 发 ClientHello,断言被嗅探并路由到正确出口(域名规则 + `final` 兜底)。纯本地、`python3` + 官方 sing-box(钉死 SHA256 下载),CI 新增 `functional` job 跑它。

## 2026-06-22 — 安全与健壮性加固(社区评审采纳)

- **防火墙改独立表 `inet pdg`,不再 `flush ruleset`**:只 declare+delete 重建本表,不清掉 Docker / fail2ban / WireGuard 等其它表;install 备份原 `/etc/nftables.conf`、uninstall 删本表并还原。
- **收紧凭据权限**:`/etc/sing-box` 改 700,`config.json` / `.botbak` / 写入临时文件统一 600(含出口密码、uuid)。
- **规则集原子更新**:`refresh_rulesets` 改为 下临时文件 → 原子替换(留 .bak)→ `sing-box check` 通过才重启,坏档自动回滚、不重启,避免每日定时遇坏 `.srs` 断网。
- **卸载更干净**:uninstall 还原 systemd-resolved 与 `resolv.conf`(install 已备份)。
- **CI ShellCheck 改为阻断**(有 warning 即失败;systemd-analyze 仍 best-effort)。
- **修 bug**:① CGNAT `100.64.0.0/10` 被 `is_private` 误判为"危险公网"(检测已支持却自检报错)——现显式放行;② `PDG_SKIP_CERT=1` 后经 bot 首次签证书缺账户注册参数——补 `--register-unsafely-without-email`。

## 2026-06-21 — bot 分流/出口编辑

- **分流管理 → ✏️ 改出口**:选一条已有规则(域名组或规则集)直接改到别的出口,不用删了重加(同出口域名自动合并保持整洁)。
- **出口管理 → ↕️ 出口排序**:发一行新顺序即可重排出口列表。
- **出口管理 → ✏️ 改故障组**:选故障组→发新成员(空格分隔、按顺序),原地改、列表位置不变;`🔀 故障切换组` 改名为 `🔀 新建故障组`。
- **分流管理 → ✏️ 改规则集名**:给规则集起看得懂的显示名(如「币安」「OpenAI」),分流规则列表不再只显示 `rs_xxxx`;加规则集时也可在末尾直接带名称(`URL 出口 名称`)。加规则集提示改为「后缀 .list / .txt / .srs」。
- bot 发消息 HTML 解析失败时退回纯文本重试,避免出错信息(如 sing-box 报错含 `<`、`&`)导致消息+按钮静默丢失。
- **删规则改多选**:列出现有单域名(显示 `域名 → 出口`),勾选多个 → 点「✅ 确认删除(N)」**一次性删、只重启一次 sing-box**;留「✍️ 手动输入」兜底。
- **修复**:连续快速改配置(如连点删域名)会在 10 秒内多次 `restart sing-box`,撞上 systemd start-limit 把 sing-box 锁成 failed(配置本身没问题)。`apply_sb` 现在 restart 前先 `reset-failed`,且重启失败自动还原上一份配置重试,不会把代理留在挂掉状态。
- bot `answerCallbackQuery`(停按钮转圈)改后台异步,连点菜单不再每步叠加一个到 Telegram 的来回。

## 2026-06-21 — 工程化收口

不新增代理协议、不改分流语义,只做工程化与安全加固:

- **Token 迁移到 `bot.env`**:TG token / 允许 id 从 systemd unit 移到 `/etc/privdns-gateway/bot.env`(目录 700 / 文件 600),unit 改用 `EnvironmentFile=`。
  `pdg-set-token`、`healthcheck` 同步改读 bot.env;**旧装升级时自动迁移**(把 unit 里的明文 token 搬进 bot.env)。
- **`pdg update` 校验门加强**:更新前快照不变;更新后跑 `py_compile` + `sing-box check` + `nft -c` + `pdg doctor --json`。
  有 `fail` 自动回滚,`warn` 仅提示;未配置 token 时不把「pdg-bot 未运行」误判为失败。
- **新增 `pdg report`**:一条命令生成**脱敏**诊断快照(doctor / 服务 / 日志 / 版本 / 端口 / 证书 / A 记录 / 防火墙),自动隐藏 token、密码、uuid、出口链接,输出文件 600。
- **GitHub Actions CI**:`py_compile` + `bash -n` + JSON 模板渲染校验 + ShellCheck;另加 mobileconfig plist 校验 + `systemd-analyze verify`(best-effort)。纯静态,不启动服务。
- **文档**:README / INSTALL / 排障手册按句换行,便于阅读与 diff。
- **`pdg doctor --deep`**:在常规自检外追加慢速端到端检查(DoT 853 TLS 握手 / `:81` 探测 200 / mosdns 解析 / clash_api);代理劫持仅对内网卡来源生效,本机不可复现,如实标注。
- **`pdg report --redact-ip / --full`**:`--redact-ip` 连公网 IP、内网 CIDR、DoT 域名一并隐藏(贴公开 issue 用);`--full` 不脱敏仅本机看。默认行为与 600 权限不变。
- **bot 主菜单**:「📊 状态」按钮改为「🔄 更新」(检查→确认→后台 `systemd-run` 执行,不被自身重启打断)。
- **并发加锁**:`pdg update / rollback / snapshot` 用 `flock`(`/run/privdns-gateway.lock`)串行化,防 bot 更新按钮与命令行同时操作。
- **内网卡识别增强**:抓包过滤补 CGNAT `100.64.0.0/10`、改抓"打到网关服务的包"(不限 SYN,已连的 DoT 也能抓);新增 **`pdg detect-cidr`**——装完随时从容重测,与现配不符可一键写回 mosdns+nftables 并重启。安装时识别失败的提示改为引导用它。
- **防火墙拒 QUIC**:对内网卡来源的 **UDP/443 改为 `reject`**(原先放行),逼客户端回落 TCP/443(才能被嗅 SNI 分流),也避免 UDP 443 进 sing-box 自环。
- **Telegram 独立 SOCKS5**:sing-box 加一个仅内网卡可达的 `mixed` 入口(`:8445`),Telegram 内置代理填 `网关IP:8445` 即可(Telegram 走直连 IP、不吃 DNS+SNI 分流);出口可在 bot『📱客户端→✈️Telegram 出口』单独选(默认跟随「默认出口」)。
- **文档**:QUICKSTART 新增「局限与补丁」节(Speedtest/纯 UDP/直连 IP/Telegram 不走这套及兜底思路);新手图文教程(含示意配图)+ README 顶部入口。

## 2026-06-20 — 首个公开版本

### 网关核心
- **DNS 层 mosdns**:国内直连 / 代理域名 A 记录劫持到本机 + AAAA·HTTPS 置空 / 按来源 IP 分支 / ECS 分治 / 响应缓存;DoT(853)。
- **流量层 sing-box 1.12**:`direct` 监听 + `sniff_override_destination`(不用 tproxy);多出口,urltest 故障切换;clash_api。
- **一键安装** `install.sh`(自动识别公网 IP / 内网卡段,DNS 那步留用户)、`uninstall.sh`。

### 管理
- **`pdg` CLI**:`status` / `doctor` / `update [--dry-run]` / `snapshot` / `rollback` / `token` / `restart` / `log` / `traffic` / `ios` / `uninstall`。
- **Telegram bot**:出口(ss/vmess/trojan/vless)、故障切换组、分流规则、Surge 规则集、🔎测域名、测出口、流量、DNS 上游、TFO、配置备份/恢复、iOS 描述文件下发、自定义 DoT 域名。
  改 sing-box 前 check + 自动回滚。

### 可靠性与运维
- **`pdg doctor`** 只读自检(服务 / sing-box 版本 / DoT A 记录 / dot-domain 一致性 / 内网卡段 / 防火墙 / 证书 / 本机 DNS / sing-box check),支持 `--json`。
- **健康自检告警**:`pdg-health.timer` 每 10 分钟跑,异常 Telegram 私信(仅状态变化)。
- **snapshot / rollback**:整机配置 + 防火墙 + service 快照到 `/var/lib/privdns-gateway/backups`(留最近 10 份);
  `pdg update` 更新前自动快照、失败自动回滚。
- **配置备份/恢复机器感知**:跨机导入只搬出口/分流/规则集,本机 IP/证书路径/内网卡段保留。
- **证书** Let's Encrypt 自动续期(已处理续期时 80 口被 sing-box 占用的问题);**vnstat** 网卡流量统计。

### 安全
- nftables 暴露面收敛:对全网仅 SSH;`53/80/81/443/853` 仅放行内网卡来源段。

> ⚠️ sing-box 必须 1.12.x:1.13+ 移除了 `sniff_override_destination`,本网关会失效。详见 [docs/INSTALL.md](docs/INSTALL.md)。
